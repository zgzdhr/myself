# Phase 3 真实可试用 MVP 稳定化执行地图

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` or `superpowers:executing-plans` to implement this plan task-by-task. Follow the checkbox steps. Do not skip verification.

**Goal:** 把当前项目从“已有 MVP 骨架和第一版功能”推进到“真实 DeepSeek 解析稳定、任务更新可控、记忆解释清楚、用户可以连续试用几天”的状态。

**Architecture:** 保持 local-first。移动端 Flutter + Riverpod + Drift/SQLite 负责输入、确认、自动保存、任务更新和记忆管理；API 侧 TypeScript + Zod + DeepSeek proxy 负责结构化 JSON 解析和 schema 校验；ContextBuilder 只做受控上下文选择，不做完整 Agent、向量检索或自动长期画像进化。

**Tech Stack:** Flutter, Dart, Riverpod, Drift, SQLite, TypeScript, Express, Zod, DeepSeek API proxy, Node test, Flutter test.

---

## 0. 当前状态校准

这份文件是后续 AI Agent 从 A 到 E 依次执行的工作地图。开始任何任务前，先确认它不是空项目。

2026-06-07 状态更新：

- A 阶段、B 阶段、C 阶段已经完成并提交到 `main`。
- C 阶段完成内容包括：待确认卡片显示保存目标、首页建议显示引用来源、自动保存边界回归测试、记忆入口增强。
- D0 中文语义边界校准已经完成：新增正式规则文档，更新 parser prompt/sample/smoke，真实 DeepSeek smoke 44 条全部通过。
- 当前下一步是 D1：`current_suggestion` 排序和解释优化。
- D0 的执行入口和交接参考是 `2026-06-07-phase3_d0_guide.md` 与 `docs/architecture/chinese-semantic-classification-rules.md`。

当前已经存在：

- `apps/mobile` Flutter Android/iOS App。
- `apps/api` TypeScript API proxy。
- 7 张 MVP 表：`raw_inputs`、`ai_parse_results`、`extracted_items`、`tasks`、`short_term_states`、`life_events`、`profile_items`。
- DeepSeek parser service、Zod schema、parser prompt、parser sample set。
- Mock Parser 和 HTTP Parser Client。
- 万能输入页、待确认卡片、编辑弹层、确认 / 修改 / 拒绝控制器。
- 自动保存第一版：近期任务、短期状态、低风险 life_event 可自动入库并可撤销；`profile_candidate` 仍必须用户确认。
- `task_update` 第一版：已支持 `ready` / `needsSelection` / `noMatch`，并可执行 complete / cancel / delay / edit。
- 首页建议第一版：基于当前任务、有效短期状态、已确认长期画像。
- `ContextBuilder` 已支持 `current_suggestion` 和 `task_update_resolution`。
- 记忆管理页和隐私页第一版。
- 移动端和 API 的基础测试。

当前必须遵守的边界：

- 不重新创建 `apps/mobile` 或 `apps/api`。
- 不新增 MVP item type。
- 不做完整 AI Agent。
- 不做向量检索。
- 不做自动长期画像进化。
- 不做复杂日/周/月复盘。
- 不做云同步。
- 不做语音输入或语音回复。
- 不把 `profile_candidate` 自动写成正式长期画像。

开始每个任务前必须先运行：

```bash
git status --short --branch
```

如果有无关未提交改动，不要覆盖；先说明并只改当前任务相关文件。

---

## 1. 建议总顺序

按下面顺序执行，不要一次交给 Agent 做多个大阶段：

```text
A. API 真实解析稳定化
B. task_update 规则收口与 ContextBuilder 扩展
C. 解释性 UI 与用户控制感
D0. 中文语义边界校准
D. ContextBuilder 小步增强
E. 稳定化、真实试用与交接
```

最推荐的实际执行节奏：

```text
A1 -> A2 -> A3 -> A4 -> A5
B1 -> B2 -> B3 -> B4
C1 -> C2 -> C3 -> C4
D1 -> D2
E1 -> E2 -> E3 -> E4
```

每个任务完成后都要小步提交。提交前必须按项目 `AGENTS.md` 规则输出提交总结，并把中文总结写进 commit body。

---

# A. API 真实解析稳定化

这一阶段的目标是让真实 DeepSeek 对中文输入稳定输出可用 JSON。先压住 parser，再继续扩 UI 和任务更新。

## A1. 扩展中文 parser sample set

**目标:** 把 API sample set 从当前少量样例扩展到至少 30 条中文真实输入。

**Files:**
- Modify: `apps/api/src/services/parserSampleSet.ts`
- Modify: `apps/api/test/parserSampleSet.test.ts`
- Modify: `docs/architecture/phase-2-ai-parse-smoke.md`
- Optional Modify: `docs/architecture/deepseek-smoke-test.md`

**必须覆盖:**

- `task_create`
- `task_update`
- `short_term_state`
- `life_event`
- `general_answer`
- `profile_candidate`
- 一句话多个意图
- 模糊时间
- 一次性情绪
- 普通闲聊
- 出差 / 客户 / 项目真实场景
- 容易误判的长期画像边界

**关键规则:**

- `profile_candidate` 样例必须检查 `need_user_confirm=true`。
- 普通问答不应保存为任务或画像。
- 一次性情绪不能变成长期画像。
- 模糊时间应保留原文，不鼓励乱造 ISO。
- `task_update` 样例要覆盖 complete / cancel / delay / edit。

- [ ] Step 1: 读取当前 sample 和测试

```bash
sed -n '1,220p' apps/api/src/services/parserSampleSet.ts
sed -n '1,220p' apps/api/test/parserSampleSet.test.ts
```

- [ ] Step 2: 扩展样例到至少 30 条

每条样例保持结构清楚：

```ts
{
  id: "multi_intent_task_state_profile",
  label: "多意图：任务、短期状态、长期偏好",
  text: "明天上午联系王总，我今天很累，我不喜欢太频繁的提醒。",
  expectedTypes: ["task_create", "short_term_state", "profile_candidate"],
  forbiddenTypes: [],
}
```

边界样例可以使用 `acceptedTypes`：

```ts
{
  id: "one_off_customer_emotion",
  label: "一次性客户情绪，不应变长期画像",
  text: "今天客户说话让我有点不舒服，但可能只是今天这一次。",
  expectedTypes: [],
  acceptedTypes: ["short_term_state", "life_event"],
  forbiddenTypes: ["profile_candidate"],
}
```

- [ ] Step 3: 补 sample set 测试

测试至少检查：

- 样例数量不少于 30。
- id 不重复。
- expected / accepted / forbidden type 都是 MVP 六类之一。
- 每类核心 item type 至少出现 2 次。
- `task_update` 至少覆盖 complete / cancel / delay / edit 的输入语义。

- [ ] Step 4: 运行 API 测试

```bash
cd apps/api
npm run typecheck
npm test
```

Expected: all pass.

- [ ] Step 5: 更新 smoke 文档并提交

提交标题建议：

```bash
git add apps/api/src/services/parserSampleSet.ts apps/api/test/parserSampleSet.test.ts docs/architecture/phase-2-ai-parse-smoke.md docs/architecture/deepseek-smoke-test.md
git commit -m "test: expand parser Chinese smoke samples" -m "本次提交小结：\n\n- 将 DeepSeek parser 中文样例扩展到至少 30 条。\n- 覆盖 task_create、task_update、short_term_state、life_event、general_answer、profile_candidate。\n- 补充多意图、模糊时间、一次性情绪、出差客户项目场景。\n- 增加 sample set 结构测试，避免样例类型漂移。\n- 本次不修改移动端功能，不新增 MVP item type。\n- 验证：cd apps/api && npm run typecheck && npm test。"
```

---

## A2. 增强 parser prompt

**目标:** 让 DeepSeek 更稳定输出符合 schema 的 JSON，减少误判。

**Files:**
- Modify: `apps/api/src/services/parserPrompt.ts`
- Modify: `apps/api/test/parserPrompt.test.ts`
- Optional Modify: `docs/architecture/phase-2-ai-parse-smoke.md`

**必须强化的 prompt 规则:**

- 只输出 JSON。
- 只允许 6 种 MVP item type。
- 不要把一次性情绪变成长期画像。
- 模糊时间保留 `due_time_text`；不确定不要填 `due_time_iso`。
- `profile_candidate.need_user_confirm` 必须为 `true`。
- 普通问答不默认保存。
- `source_text` 必须引用用户原文片段。
- `task_update` 必须包含 `update_action`，并尽量提供 `target_task_title` 或 `target_text`。
- 不确定时降低 `confidence`。
- 不要为了凑结构强行生成 item。

- [ ] Step 1: 读取 prompt 和测试

```bash
sed -n '1,240p' apps/api/src/services/parserPrompt.ts
sed -n '1,240p' apps/api/test/parserPrompt.test.ts
```

- [ ] Step 2: 先写 prompt test

测试应明确匹配这些关键词或规则文本：

```ts
assert.match(prompt, /JSON/);
assert.match(prompt, /profile_candidate/);
assert.match(prompt, /need_user_confirm/);
assert.match(prompt, /source_text/);
assert.match(prompt, /task_update/);
assert.match(prompt, /update_action/);
assert.match(prompt, /due_time_text/);
assert.match(prompt, /Do not treat one-time emotions/i);
```

- [ ] Step 3: 修改 prompt

保持 prompt 简洁、规则明确，不要加入复杂 Agent 指令。

- [ ] Step 4: 运行 API 测试

```bash
cd apps/api
npm run typecheck
npm test
```

Expected: all pass.

- [ ] Step 5: 提交

```bash
git add apps/api/src/services/parserPrompt.ts apps/api/test/parserPrompt.test.ts docs/architecture/phase-2-ai-parse-smoke.md
git commit -m "fix: tighten parser prompt boundaries" -m "本次提交小结：\n\n- 强化 DeepSeek parser prompt 的 JSON、schema 和 MVP 类型边界。\n- 明确 profile_candidate 必须用户确认后才生效。\n- 明确一次性情绪不能被当作长期画像。\n- 明确 task_update 需要 update_action 和目标任务文本。\n- 本次不修改数据库，不扩展 MVP 类型。\n- 验证：cd apps/api && npm run typecheck && npm test。"
```

---

## A3. 增强 parser smoke 规则检查

**目标:** `npm run smoke:parse` 不只看“返回 JSON”，还要看类型、关键字段和边界规则是否正确。

**Files:**
- Modify: `apps/api/scripts/smokeParseSamples.ts`
- Modify: `apps/api/test/smokeParseSamples.test.ts`
- Modify: `apps/api/src/services/parserSampleSet.ts`
- Modify: `docs/architecture/phase-2-ai-parse-smoke.md`

**检查项:**

- `expectedTypes`
- `acceptedTypes`
- `forbiddenTypes`
- `profile_candidate.need_user_confirm === true`
- `source_text` 非空
- `confidence` 范围
- 普通问答不能生成 task/profile
- 模糊时间不要乱造 ISO
- 输出统计：checked / pass / need review / schema error

- [ ] Step 1: 先扩展 `SmokeSampleResult`

建议包含：

```ts
export type SmokeSampleResult = {
  sampleId: string;
  label: string;
  ok: boolean;
  actualTypes: ParserItemType[];
  missingTypes: ParserItemType[];
  forbiddenTypes: ParserItemType[];
  ruleFailures: string[];
};
```

- [ ] Step 2: 补测试

测试至少覆盖：

- 缺 expected type 会 fail。
- 出现 forbidden type 会 fail。
- profile candidate 未确认会 fail。
- source_text 为空会 fail。
- confidence 越界会 fail。

- [ ] Step 3: 更新 CLI 输出

目标输出类似：

```text
30 samples checked
28 pass
2 need review
0 schema error
```

- [ ] Step 4: 运行 API 验证

```bash
cd apps/api
npm run typecheck
npm test
```

如果有真实 API key 和本地 API 已启动，再运行：

```bash
npm run smoke:parse
```

- [ ] Step 5: 提交

```bash
git add apps/api/scripts/smokeParseSamples.ts apps/api/test/smokeParseSamples.test.ts apps/api/src/services/parserSampleSet.ts docs/architecture/phase-2-ai-parse-smoke.md
git commit -m "test: strengthen parser smoke assertions" -m "本次提交小结：\n\n- 增强 parser smoke 对 expected、accepted、forbidden 类型的检查。\n- 增加 profile_candidate 用户确认、source_text、confidence 等关键规则检查。\n- 改进 smoke 输出，让真实解析问题更容易定位。\n- 本次不修改移动端功能。\n- 验证：cd apps/api && npm run typecheck && npm test。"
```

---

## A4. API 与移动端失败兜底复查

**目标:** 确认真实解析失败时，用户不看到技术错误，日志不泄漏 raw input。

**Files:**
- Inspect/Modify: `apps/api/src/routes/parse.ts`
- Inspect/Modify: `apps/api/src/services/deepseekParser.ts`
- Inspect/Modify: `apps/api/test/deepseekParser.test.ts`
- Inspect/Modify: `apps/api/test/privacyLogging.test.ts`
- Inspect/Modify: `apps/mobile/lib/data/parser/http_parser_client.dart`
- Inspect/Modify: `apps/mobile/lib/features/input/input_screen.dart`
- Inspect/Modify: `apps/mobile/test/features/memory/privacy_failure_test.dart`
- Inspect/Modify: `docs/architecture/phase-2-ai-parse-smoke.md`

**需要覆盖的失败路径:**

- API key 缺失。
- DeepSeek 超时。
- DeepSeek HTTP error。
- JSON parse 失败。
- schema validation 失败。
- 移动端网络错误。
- 用户输入为空。
- 用户输入过长。

- [ ] Step 1: 先读现有错误处理和测试
- [ ] Step 2: 只补缺口，不重写已有错误体系
- [ ] Step 3: 用户侧文案保持稳定

建议文案：

```text
这次我没有整理成功，可以稍后重试。
你的原始输入还没有被保存为正式记忆。
```

- [ ] Step 4: 运行验证

```bash
cd apps/api
npm run typecheck
npm test

cd ../mobile
flutter analyze
flutter test
```

- [ ] Step 5: 提交

```bash
git add apps/api apps/mobile docs/architecture/phase-2-ai-parse-smoke.md
git commit -m "fix: harden parser failure fallbacks" -m "本次提交小结：\n\n- 复查并补齐 DeepSeek parser 失败兜底路径。\n- 确保移动端失败提示不暴露底层异常。\n- 确保 API 日志不记录用户原始输入。\n- 本次不新增业务功能，只增强失败安全性。\n- 验证：apps/api typecheck/test；apps/mobile analyze/test。"
```

---

## A5. 真实 DeepSeek 与移动端 API 手动验证

**目标:** 确认移动端不是只在 mock 模式下可用，而是能通过本地 API proxy 调真实 DeepSeek。

**Files:**
- Modify: `docs/architecture/mobile-smoke-test.md`
- Modify: `docs/architecture/phase-2-ai-parse-smoke.md`
- Optional Modify: `.env.example`
- Optional Modify: `README.md`

**不在本任务中做:**

- 不改 parser prompt。
- 不改数据库 schema。
- 不新增功能。

- [ ] Step 1: 检查环境

```bash
cd apps/mobile
flutter doctor
flutter devices
```

- [ ] Step 2: 启动 API

```bash
cd apps/api
npm run dev
```

- [ ] Step 3: 运行移动端

iOS:

```bash
cd apps/mobile
flutter run -d <ios-simulator-id> --dart-define=API_BASE_URL=http://127.0.0.1:8787
```

Android emulator 可能需要使用宿主机地址：

```bash
cd apps/mobile
flutter run -d <android-emulator-id> --dart-define=API_BASE_URL=http://10.0.2.2:8787
```

- [ ] Step 4: 手动输入至少 10 条样例

必须包括：

- 明确今日/明日任务。
- 短期状态。
- 长期画像候选。
- 普通问答。
- 一句话多意图。
- task_update complete。
- task_update delay。
- task_update 多候选。
- task_update 无匹配。
- 解析失败或网络失败场景。

- [ ] Step 5: 更新文档

记录：

- 日期。
- 设备。
- API 地址。
- 是否真实 DeepSeek。
- 10 条输入的结果摘要。
- 未解决问题。

- [ ] Step 6: 提交

```bash
git add docs/architecture/mobile-smoke-test.md docs/architecture/phase-2-ai-parse-smoke.md README.md .env.example
git commit -m "docs: record real parser mobile smoke results" -m "本次提交小结：\n\n- 记录移动端通过本地 API proxy 调用真实 DeepSeek 的手动验证结果。\n- 覆盖任务、短期状态、长期画像候选、普通问答和 task_update 场景。\n- 记录 Android/iOS 环境、API_BASE_URL 和未解决问题。\n- 本次不修改产品功能。\n- 验证：手动移动端 smoke。"
```

---

# B. task_update 规则收口与 ContextBuilder 扩展

重要校准：`task_update` 不是从零开始。当前已有第一版控制器逻辑和测试。B 阶段目标是把规则写清楚、把上下文来源收口到 ContextBuilder、补 UI 和真实验证，不要重复造一套任务更新系统。

## B1. 写 task_update resolution 规则文档

**目标:** 先把任务更新规则写清楚，避免 AI 或后续 Agent 乱改任务。

**Files:**
- Create: `docs/architecture/task-update-resolution.md`
- Inspect: `apps/mobile/lib/features/extracted_items/extracted_items_controller.dart`
- Inspect: `apps/mobile/lib/domain/extracted_item.dart`
- Inspect: `apps/mobile/test/features/extracted_items/confirmation_flow_test.dart`

**必须明确:**

- 什么情况下 `ready`。
- 什么情况下 `needsSelection`。
- 什么情况下 `noMatch`。
- 多个任务标题相似怎么办。
- 用户说“那个事”怎么办。
- 用户只说“完成了”但没有目标怎么办。
- 延期是否必须有新时间。
- 取消和删除的区别。
- complete 当前是否使用 `archived`。
- cancel 当前是否使用 `deleted`。
- 是否要引入 `task_status`。

**推荐结论:**

短期可以继续使用现有 schema，但必须在文档里标明风险：

```text
当前 MVP 中 complete -> archived，cancel -> deleted。
这是为了避免立刻改 schema。后续更推荐给 tasks 增加 task_status: active/completed/cancelled。
record status 继续表示记录生命周期，task_status 表示任务业务状态。
```

- [ ] Step 1: 阅读现有 task_update 代码和测试
- [ ] Step 2: 创建文档并写明现有行为
- [ ] Step 3: 写明未来推荐 schema 方向，但本任务不改 schema
- [ ] Step 4: 提交

```bash
git add docs/architecture/task-update-resolution.md
git commit -m "docs: define task update resolution rules" -m "本次提交小结：\n\n- 新增 task_update 匹配、选择、无匹配和执行规则文档。\n- 明确当前 MVP 使用 archived 表示完成、deleted 表示取消的临时边界。\n- 记录未来更推荐的 task_status 方向。\n- 本次只写规则文档，不修改代码和数据库 schema。\n- 验证：文档审阅。"
```

---

## B2. ContextBuilder 新增 taskUpdateResolution intent

**目标:** 让任务更新所需的 active task 上下文由 ContextBuilder 统一提供，而不是散落在控制器内部。

**Files:**
- Modify: `apps/mobile/lib/features/context/context_builder.dart`
- Modify: `apps/mobile/test/features/context/context_builder_test.dart`
- Modify: `apps/mobile/lib/features/extracted_items/extracted_items_controller.dart`
- Modify: `apps/mobile/test/features/extracted_items/confirmation_flow_test.dart`
- Modify: `docs/architecture/context-builder-design.md`
- Reference: `docs/architecture/task-update-resolution.md`

**建议模型:**

```dart
enum ContextIntent {
  currentSuggestion,
  taskUpdateResolution;
}

class ContextTaskCandidate {
  const ContextTaskCandidate({
    required this.id,
    required this.title,
    required this.priority,
    this.dueTimeText,
    this.dueTime,
  });

  final String id;
  final String title;
  final String priority;
  final String? dueTimeText;
  final DateTime? dueTime;
}
```

**边界:**

- 只包含 active/confirmed tasks。
- 排除 deleted / archived。
- 不传 raw input。
- debug json 不包含敏感正文。
- 不传 life events、profile、summary。

- [ ] Step 1: 先写 ContextBuilder 测试

测试覆盖：

- confirmed task 被包含。
- deleted task 被排除。
- archived task 被排除。
- candidate 只包含 id/title/priority/dueTimeText/dueTime。
- debug json 不包含 raw input。

- [ ] Step 2: 实现 `buildTaskUpdateResolution`
- [ ] Step 3: 让 controller 使用 ContextBuilder 返回的 active tasks
- [ ] Step 4: 运行移动端验证

```bash
cd apps/mobile
flutter analyze
flutter test
```

- [ ] Step 5: 提交

```bash
git add apps/mobile/lib/features/context/context_builder.dart apps/mobile/test/features/context/context_builder_test.dart apps/mobile/lib/features/extracted_items/extracted_items_controller.dart apps/mobile/test/features/extracted_items/confirmation_flow_test.dart docs/architecture/context-builder-design.md
git commit -m "feat: add task update context builder intent" -m "本次提交小结：\n\n- ContextBuilder 新增 task_update_resolution 上下文意图。\n- 为任务更新提供最小 active task candidate 包。\n- 排除 deleted 和 archived task，避免误更新无效任务。\n- 控制器复用 ContextBuilder 上下文，不再自己散落查询任务上下文。\n- 本次不做向量检索，不新增 Agent 能力。\n- 验证：cd apps/mobile && flutter analyze && flutter test。"
```

---

## B3. 收紧 task_update 候选匹配规则

**目标:** 在现有匹配逻辑基础上补齐边界，降低误更新风险。

**Files:**
- Modify: `apps/mobile/lib/features/extracted_items/extracted_items_controller.dart`
- Modify: `apps/mobile/test/features/extracted_items/confirmation_flow_test.dart`
- Modify: `docs/architecture/task-update-resolution.md`

**当前已有:**

- exact match。
- title contains target。
- 0/1/多候选 resolution。

**建议补强:**

- target 为空时直接 noMatch。
- delay 没有新时间时 needsSelection 或 noMatch，不能静默清空 due time。
- deleted / archived task 不参与匹配。
- target 包含泛词“那个事”“这个任务”时，若没有明确目标，不要猜。
- 多候选必须用户选择。

- [ ] Step 1: 先写失败测试

至少新增：

- archived task 不参与匹配。
- deleted task 不参与匹配。
- target 为空 noMatch。
- delay 缺少新时间不直接更新。
- “那个事完成了”没有明确目标时 noMatch。

- [ ] Step 2: 修改匹配逻辑
- [ ] Step 3: 运行测试

```bash
cd apps/mobile
flutter analyze
flutter test
```

- [ ] Step 4: 提交

```bash
git add apps/mobile/lib/features/extracted_items/extracted_items_controller.dart apps/mobile/test/features/extracted_items/confirmation_flow_test.dart docs/architecture/task-update-resolution.md
git commit -m "fix: tighten task update matching rules" -m "本次提交小结：\n\n- 收紧 task_update 候选匹配，减少误更新正式任务的风险。\n- 补充空目标、泛指目标、无新时间延期、deleted/archived 排除等边界测试。\n- 多候选继续要求用户选择，不让 AI 静默猜测。\n- 本次不新增数据库字段。\n- 验证：cd apps/mobile && flutter analyze && flutter test。"
```

---

## B4. task_update UI 体验补强

**目标:** 让用户在 ready / needsSelection / noMatch 三种场景下看得懂、能选择、能取消。

**Files:**
- Modify: `apps/mobile/lib/features/input/input_screen.dart`
- Modify: `apps/mobile/lib/features/extracted_items/extracted_item_card.dart`
- Modify: `apps/mobile/test/features/extracted_items/extracted_item_card_test.dart`
- Optional Modify: `apps/mobile/test/widget_test.dart`

**场景文案:**

ready:

```text
我理解你想更新：联系王总
将标记为已完成
[确认更新] [拒绝]
```

needsSelection:

```text
你说的是哪一个任务？
联系王总-上海
联系王总-北京
```

noMatch:

```text
我没有找到对应任务。
这次不会修改任何任务。
[关闭]
```

- [ ] Step 1: 补 widget test
- [ ] Step 2: 修改 UI 文案和按钮行为
- [ ] Step 3: 确认 noMatch 不会提供误导性的“新建任务”默认动作，除非另开任务设计
- [ ] Step 4: 运行验证

```bash
cd apps/mobile
flutter analyze
flutter test
```

- [ ] Step 5: 提交

```bash
git add apps/mobile/lib/features/input/input_screen.dart apps/mobile/lib/features/extracted_items/extracted_item_card.dart apps/mobile/test/features/extracted_items/extracted_item_card_test.dart apps/mobile/test/widget_test.dart
git commit -m "fix: clarify task update confirmation UI" -m "本次提交小结：\n\n- 优化 task_update 在唯一匹配、多候选、无匹配下的用户提示。\n- 多候选必须用户选择目标任务。\n- 无匹配场景明确说明不会修改任何任务。\n- 补充 widget test，保护任务更新 UI 行为。\n- 验证：cd apps/mobile && flutter analyze && flutter test。"
```

---

# C. 解释性 UI 与用户控制感

这一阶段目标是让用户知道 AI 做了什么、为什么这么做、自己能否改。

## C1. 待确认卡片显示保存目标和确认边界

**目标:** 卡片不仅显示来源，还显示会保存到哪里、是否已经自动保存、长期画像是否必须确认。

**Files:**
- Modify: `apps/mobile/lib/features/extracted_items/extracted_item_card.dart`
- Modify: `apps/mobile/test/features/extracted_items/extracted_item_card_test.dart`

**显示规则:**

- `task_create` -> 保存到：任务。
- `short_term_state` -> 保存到：短期状态。
- `life_event` -> 保存到：生活事件。
- `profile_candidate` -> 保存到：长期画像；确认后才生效。
- `task_update` -> 目标：更新已有任务，不新建记忆。
- 自动保存状态显示：已自动整理，可修改或撤销。

- [ ] Step 1: 写 widget test
- [ ] Step 2: 增加保存目标文案
- [ ] Step 3: 运行验证

```bash
cd apps/mobile
flutter analyze
flutter test
```

- [ ] Step 4: 提交

```bash
git add apps/mobile/lib/features/extracted_items/extracted_item_card.dart apps/mobile/test/features/extracted_items/extracted_item_card_test.dart
git commit -m "feat: show extracted item save target" -m "本次提交小结：\n\n- 待确认卡片增加保存目标说明。\n- 长期画像候选明确提示确认后才会生效。\n- 自动保存条目继续显示可修改、可撤销提示。\n- 补充 widget test 保护卡片解释性文案。\n- 验证：cd apps/mobile && flutter analyze && flutter test。"
```

---

## C2. 首页建议显示为什么推荐

**目标:** 首页建议不只显示一句建议，还能说明用了哪些当前任务、短期状态或长期偏好。

**Files:**
- Modify: `apps/mobile/lib/features/home/home_suggestion_service.dart`
- Modify: `apps/mobile/lib/features/home/home_screen.dart`
- Modify: `apps/mobile/test/features/home/home_suggestion_service_test.dart`
- Optional Modify: `apps/mobile/test/widget_test.dart`

**建议数据结构:**

如果当前已有 `Suggestion` 类型，给它增加 reason；如果没有，创建轻量 model：

```dart
class HomeSuggestion {
  const HomeSuggestion({
    required this.text,
    required this.reason,
  });

  final String text;
  final String reason;
}
```

**reason 规则:**

- 可以提任务标题、短期状态内容、长期画像内容。
- 不暴露 raw input。
- 无任务时提示用户补充今天最重要的事。

- [ ] Step 1: 先写 service test
- [ ] Step 2: 修改 service 输出 reason
- [ ] Step 3: 修改首页展示
- [ ] Step 4: 运行验证

```bash
cd apps/mobile
flutter analyze
flutter test
```

- [ ] Step 5: 提交

```bash
git add apps/mobile/lib/features/home/home_suggestion_service.dart apps/mobile/lib/features/home/home_screen.dart apps/mobile/test/features/home/home_suggestion_service_test.dart apps/mobile/test/widget_test.dart
git commit -m "feat: explain home suggestions" -m "本次提交小结：\n\n- 首页建议增加为什么推荐的说明。\n- reason 基于当前任务、有效短期状态和已确认长期画像生成。\n- 避免展示 raw input 或敏感调试信息。\n- 补充首页建议 service 测试。\n- 验证：cd apps/mobile && flutter analyze && flutter test。"
```

---

## C3. 自动保存体验复查

**目标:** 当前已有自动保存第一版，本任务只验证和打磨可见、可修改、可撤销体验。

**Files:**
- Inspect/Modify: `apps/mobile/lib/features/extracted_items/extracted_items_controller.dart`
- Inspect/Modify: `apps/mobile/lib/features/extracted_items/extracted_item_card.dart`
- Inspect/Modify: `apps/mobile/lib/features/input/input_screen.dart`
- Inspect/Modify: `apps/mobile/test/features/extracted_items/confirmation_flow_test.dart`
- Inspect/Modify: `apps/mobile/test/features/extracted_items/extracted_item_card_test.dart`

**必须确认:**

- 近期任务自动保存后可以撤销。
- 短期状态自动保存后可以撤销。
- 撤销后不参与首页建议。
- 自动保存后可编辑。
- `profile_candidate` 不自动保存。
- `general_answer` 不生成保存卡片。

- [ ] Step 1: 读现有自动保存测试
- [ ] Step 2: 只补缺失测试或文案
- [ ] Step 3: 运行验证

```bash
cd apps/mobile
flutter analyze
flutter test
```

- [ ] Step 4: 提交

```bash
git add apps/mobile/lib/features/extracted_items apps/mobile/lib/features/input apps/mobile/test/features/extracted_items
git commit -m "fix: verify auto-save undo experience" -m "本次提交小结：\n\n- 复查自动保存后的可见、可修改、可撤销体验。\n- 确认撤销后的任务和短期状态不再参与首页建议。\n- 确认长期画像候选和普通问答不会被自动保存为正式记忆。\n- 本次只做体验和测试补强，不改变 MVP 边界。\n- 验证：cd apps/mobile && flutter analyze && flutter test。"
```

---

## C4. 记忆入口增强

**目标:** 用户能回答“这个 App 现在到底记住了我什么？”

**Files:**
- Modify: `apps/mobile/lib/features/memory/memory_screen.dart`
- Modify: `apps/mobile/lib/features/memory/tasks_screen.dart`
- Modify: `apps/mobile/lib/features/memory/short_term_states_screen.dart`
- Modify: `apps/mobile/lib/features/memory/life_events_screen.dart`
- Modify: `apps/mobile/lib/features/memory/profile_items_screen.dart`
- Modify: `apps/mobile/test/features/memory/memory_management_test.dart`

**建议增强:**

- 每类显示数量。
- 每类显示最近更新时间。
- 显示是否有 pending。
- 保持删除/编辑能力。
- 不新增复杂解析历史页，除非单独开任务。

- [ ] Step 1: 先写 memory management test
- [ ] Step 2: 修改记忆入口 UI
- [ ] Step 3: 运行验证

```bash
cd apps/mobile
flutter analyze
flutter test
```

- [ ] Step 4: 提交

```bash
git add apps/mobile/lib/features/memory apps/mobile/test/features/memory/memory_management_test.dart
git commit -m "feat: improve memory overview" -m "本次提交小结：\n\n- 记忆入口增强各类记忆的数量和最近更新时间展示。\n- 保持任务、短期状态、生活事件、长期画像的查看和删除边界。\n- 帮用户更清楚看到 App 当前记住了什么。\n- 补充记忆管理测试。\n- 验证：cd apps/mobile && flutter analyze && flutter test。"
```

---

# D. ContextBuilder 小步增强

这一阶段只做明确场景的上下文选择，不做通用大脑。

## D0. 中文语义边界校准

**目标:** 在继续优化 `current_suggestion` 之前，先把中文自然语言分类边界写清楚，并把 parser prompt、sample set、smoke 检查建立在这套边界上。

**状态:** 已完成，commit 见 D0 阶段提交。真实 DeepSeek smoke 使用新启动的 `API_PORT=8791` API proxy 运行，44 samples checked，44 pass，0 type mismatch，0 schema error。

**当前执行入口:**
- Read: `2026-06-07-phase3_d0_guide.md`
- Source drafts: `2026-06-07-phase3_d0.md`
- Source drafts: `2026-06-07-phase3_d0_reward.md`
- Source drafts: `2026-06-07-phase3_d0_update.md`

**Files for D0 implementation:**
- Create: `docs/architecture/chinese-semantic-classification-rules.md`
- Modify: `apps/api/src/services/parserPrompt.ts`
- Modify: `apps/api/test/parserPrompt.test.ts`
- Modify: `apps/api/src/services/parserSampleSet.ts`
- Modify: `apps/api/test/parserSampleSet.test.ts`
- Modify: `apps/api/scripts/smokeParseSamples.ts`
- Modify: `apps/api/test/smokeParseSamples.test.ts`
- Optional Modify: `docs/architecture/phase-2-ai-parse-smoke.md`
- Optional Modify: `docs/architecture/deepseek-smoke-test.md`

**必须明确:**

- 分类不是按关键词，而是按“这句话未来应该如何被使用”判断。
- `task_create` 覆盖未来安排、已答应事项、预约、社交娱乐约定和明确准备事项。
- `task_update` 是改变已有任务，多候选必须选择，无候选不能修改正式任务。
- `short_term_state` 是当前/近期状态；位置类未来应最新优先，精力/心情类可保留变化轨迹。
- `life_event` 是未来理解用户的重要经验素材，但当前不能自动升级为长期画像。
- `profile_candidate` 必须用户确认后才能生效。
- `general_answer` 当前默认只回答、不保存；未来再做个性化问答上下文。

**当前 D0 不做:**

- 不做提醒系统。
- 不做已完成/已取消任务自动清理设置。
- 不做延期任务双提醒。
- 不做自动长期画像进化。
- 不做深度建议/长对话页面。
- 不做向量检索、云同步、完整 AI Agent。

- [x] Step 1: 阅读 D0 指导文件和原始讨论稿

```bash
sed -n '1,240p' 2026-06-07-phase3_d0_guide.md
```

- [x] Step 2: 创建中文语义分类规则文档

文档应覆盖六类 item 定义、分类优先级、自动保存策略、模糊时间语义、真实中文例句、当前版本做什么和暂不做什么。

- [x] Step 3: 扩展 parser sample set

新增社交/娱乐未来安排、工作客户任务、模糊时间任务、条件型任务、任务更新、状态变化、生活事件/经验、长期画像候选、普通问答和混合输入样例。

- [x] Step 4: 更新 parser prompt

把分类原则写进 prompt，避免只靠“记得”“提醒我”“回头”等关键词判断。

- [x] Step 5: 增强 smoke 检查

检查 `expectedTypes`、`forbiddenTypes`、`profile_candidate.need_user_confirm=true`、`source_text` 非空、模糊时间不乱造 `due_time_iso`、普通问答不生成可保存记忆。

- [x] Step 6: 运行 API 验证

```bash
cd apps/api
npm run typecheck
npm test
API_BASE_URL=http://127.0.0.1:8791 npm run smoke:parse
```

- [x] Step 7: 提交

```bash
git add AGENTS.md 2026-06-05-phase3_副本.md docs/architecture/chinese-semantic-classification-rules.md docs/architecture/phase-3-d0-handoff.md docs/architecture/current-verification.md apps/api/src/services/parserPrompt.ts apps/api/test/parserPrompt.test.ts apps/api/src/services/parserSampleSet.ts apps/api/test/parserSampleSet.test.ts apps/api/scripts/smokeParseSamples.ts apps/api/test/smokeParseSamples.test.ts docs/architecture/phase-2-ai-parse-smoke.md docs/architecture/deepseek-smoke-test.md
git commit -m "feat: complete phase 3 D0 semantic calibration" -m "本次提交小结：\n\n- 完成 Phase 3 D0 中文语义边界校准。\n- 新增正式规则文档和 handoff，方便 D1/D2 继续执行。\n- 明确 task_create、task_update、short_term_state、life_event、profile_candidate、general_answer 的中文分类边界。\n- 扩展 parser prompt、sample set 和 smoke 检查，覆盖社交安排、模糊时间、生活经验和普通问答边界。\n- 更新 AGENTS.md、Phase 3 任务地图、DeepSeek smoke 文档和 current-verification。\n- 当前不做提醒系统、自动画像进化、深度建议或完整 AI Agent。\n- 验证：cd apps/api && npm run typecheck && npm test；真实 DeepSeek smoke 44 samples pass。"
```

## D1. current_suggestion 排序和解释优化

**目标:** 让首页建议更稳定地优先处理逾期、今天、未来 7 天任务，并结合短期状态和长期偏好。

**Files:**
- Modify: `apps/mobile/lib/features/context/context_builder.dart`
- Modify: `apps/mobile/lib/features/home/home_suggestion_service.dart`
- Modify: `apps/mobile/test/features/context/context_builder_test.dart`
- Modify: `apps/mobile/test/features/home/home_suggestion_service_test.dart`

**规则:**

- overdue tasks 优先。
- today tasks 高于 future tasks。
- 无 due time 的任务可以作为今天候选，但不要永远压过明确 due time。
- 低能量状态下推荐低阻力任务。
- 有“不喜欢频繁提醒”画像时文案更柔和。

- [ ] Step 1: 写排序和文案测试
- [ ] Step 2: 修改 ContextBuilder / HomeSuggestionService
- [ ] Step 3: 确认 debug json 不暴露 raw input
- [ ] Step 4: 运行验证

```bash
cd apps/mobile
flutter analyze
flutter test
```

- [ ] Step 5: 提交

```bash
git add apps/mobile/lib/features/context/context_builder.dart apps/mobile/lib/features/home/home_suggestion_service.dart apps/mobile/test/features/context/context_builder_test.dart apps/mobile/test/features/home/home_suggestion_service_test.dart
git commit -m "fix: refine current suggestion context ranking" -m "本次提交小结：\n\n- 优化 current_suggestion 的任务排序和建议文案。\n- 逾期、今天、未来 7 天任务按更清晰的优先级进入建议。\n- 结合短期状态和已确认长期偏好调整建议口吻。\n- 保持 debug 输出不暴露 raw input。\n- 验证：cd apps/mobile && flutter analyze && flutter test。"
```

---

## D2. 设计 memory_explanation，但暂不接 LLM

**目标:** 为“这个建议用了哪些记忆？”建立第一版规则解释设计。先写文档，可选实现轻量展示。

**Files:**
- Create/Modify: `docs/architecture/memory-explanation-design.md`
- Optional Modify: `apps/mobile/lib/features/context/context_builder.dart`
- Optional Modify: `apps/mobile/test/features/context/context_builder_test.dart`

**第一版只做规则解释:**

```text
这个建议使用了：
1. 已确认任务：联系王总
2. 短期状态：今天感觉疲惫
3. 长期偏好：不喜欢太频繁提醒
```

**不做:**

- 不接 LLM。
- 不做向量检索。
- 不做复杂个人问答。
- 不从 raw input 解释。

- [ ] Step 1: 写设计文档
- [ ] Step 2: 如果实现，必须先写测试
- [ ] Step 3: 运行对应验证
- [ ] Step 4: 提交

```bash
git add docs/architecture/memory-explanation-design.md apps/mobile/lib/features/context/context_builder.dart apps/mobile/test/features/context/context_builder_test.dart
git commit -m "docs: design rule-based memory explanations" -m "本次提交小结：\n\n- 新增 memory_explanation 的第一版规则解释设计。\n- 明确解释只基于用户可见、已确认、未删除的正式记录。\n- 暂不接 LLM，不做向量检索，不做复杂个人问答。\n- 为后续解释性 UI 留出清晰边界。\n- 验证：文档审阅；如改代码则运行 flutter analyze/test。"
```

---

# E. 稳定化、真实试用与交接

## E1. 增加统一检查脚本

**目标:** 根目录一条命令跑完基础验证。

**Files:**
- Create: `scripts/check-all.sh`
- Modify: `README.md`
- Modify: `docs/architecture/current-verification.md`

**脚本内容:**

```bash
#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

cd "$ROOT_DIR/apps/mobile"
flutter analyze
flutter test

cd "$ROOT_DIR/apps/api"
npm run typecheck
npm test
```

- [ ] Step 1: 创建脚本
- [ ] Step 2: 设置可执行权限

```bash
chmod +x scripts/check-all.sh
```

- [ ] Step 3: 运行脚本

```bash
./scripts/check-all.sh
```

- [ ] Step 4: 更新 README 和 verification 文档
- [ ] Step 5: 提交

```bash
git add scripts/check-all.sh README.md docs/architecture/current-verification.md
git commit -m "chore: add full project check script" -m "本次提交小结：\n\n- 新增 scripts/check-all.sh 统一运行移动端和 API 基础验证。\n- README 增加统一检查命令说明。\n- current-verification 记录最新验证方式。\n- 本次不修改业务功能。\n- 验证：./scripts/check-all.sh。"
```

---

## E2. 处理 sqlite3_flutter_libs 风险

**目标:** 评估并处理 `sqlite3_flutter_libs` 依赖风险。这个任务放在后半段，不要太早做。

**Files:**
- Inspect/Modify: `apps/mobile/pubspec.yaml`
- Inspect/Modify: `apps/mobile/pubspec.lock`
- Modify: `docs/architecture/current-verification.md`

**规则:**

- 先查当前依赖和 Flutter/Drift 兼容性。
- 如果升级会引出大范围问题，可以只记录风险，不强行升级。
- 如果升级，必须跑完整移动端测试。

- [ ] Step 1: 检查依赖

```bash
cd apps/mobile
flutter pub outdated
```

- [ ] Step 2: 判断是否升级
- [ ] Step 3: 如升级，运行

```bash
flutter pub get
flutter analyze
flutter test
```

- [ ] Step 4: 记录结果并提交

```bash
git add apps/mobile/pubspec.yaml apps/mobile/pubspec.lock docs/architecture/current-verification.md
git commit -m "chore: review sqlite flutter dependency risk" -m "本次提交小结：\n\n- 检查 sqlite3_flutter_libs 和 Drift/Flutter 依赖兼容性。\n- 根据实际情况升级或记录暂缓处理原因。\n- 更新 current-verification 中的依赖风险说明。\n- 本次不修改业务逻辑。\n- 验证：flutter pub get/analyze/test 或记录暂缓原因。"
```

---

## E3. 创建真实试用 checklist

**目标:** 让用户连续试用 3 天时知道每天要观察什么。

**Files:**
- Create: `docs/architecture/real-trial-checklist.md`

**每天记录:**

- 输入 5 条真实自然语言。
- 至少 2 条任务。
- 至少 1 条任务更新。
- 至少 1 条短期状态。
- 至少 1 条生活事件。
- 至少 1 条长期画像候选。
- 首页建议是否有用。
- 是否误保存。
- 删除/撤销是否生效。
- 哪个交互让用户不舒服。

- [ ] Step 1: 创建 checklist 文档
- [ ] Step 2: 写 3 天记录模板
- [ ] Step 3: 提交

```bash
git add docs/architecture/real-trial-checklist.md
git commit -m "docs: add real trial checklist" -m "本次提交小结：\n\n- 新增 3 天真实试用 checklist。\n- 明确每天要测试的任务、任务更新、短期状态、生活事件和长期画像候选。\n- 增加误保存、删除撤销、首页建议有效性的观察项。\n- 本次只新增文档，不修改代码。\n- 验证：文档审阅。"
```

---

## E4. 写 Phase 3 验收文档

**目标:** 阶段结束后，让项目状态可交接、可继续。

**Files:**
- Create: `docs/architecture/phase-3-verification.md`
- Modify: `docs/architecture/current-verification.md`
- Optional Modify: `AGENTS.md`
- Optional Modify: `README.md`

**内容必须包括:**

- 完成了哪些 A-E 任务。
- 哪些测试通过。
- 哪些手动 smoke 通过。
- 哪些问题暂时接受。
- 哪些任务后置。
- 下一阶段建议。

- [ ] Step 1: 运行最终验证

```bash
./scripts/check-all.sh
```

- [ ] Step 2: 如设备可用，补 Android/iOS smoke
- [ ] Step 3: 创建 `phase-3-verification.md`
- [ ] Step 4: 更新 `current-verification.md`
- [ ] Step 5: 如项目状态变化明显，更新 `AGENTS.md` 和 `README.md`
- [ ] Step 6: 提交

```bash
git add docs/architecture/phase-3-verification.md docs/architecture/current-verification.md AGENTS.md README.md
git commit -m "docs: record phase 3 verification" -m "本次提交小结：\n\n- 新增 Phase 3 验收文档，记录阶段完成内容、验证结果和遗留问题。\n- 更新 current-verification，便于后续 Agent 快速判断当前状态。\n- 如有必要，同步 README 和 AGENTS.md 的项目状态。\n- 本次为阶段交接文档提交。\n- 验证：./scripts/check-all.sh；如可用则记录移动端 smoke。"
```

---

## 2. 给后续 Agent 的总控提示词

如果你要把下一项任务交给 AI Agent，可以用这个模板：

```md
请在 /Users/mac/projects/cc/myself 中执行 Phase 3 计划里的【填入任务编号，例如 A1】。

开始前必须：
1. 阅读 AGENTS.md；
2. 阅读 2026-06-05-phase3_副本.md 中对应任务；
3. 运行 git status --short --branch；
4. 确认不重新创建 apps/mobile 或 apps/api。

任务边界：
- 只做【填入任务编号】；
- 不做完整 Agent；
- 不做向量检索；
- 不做自动长期画像进化；
- 不新增 MVP item type；
- 不覆盖无关未提交改动。

完成后必须：
1. 跑该任务要求的验证命令；
2. 更新相关文档；
3. 按 AGENTS.md 的提交规范输出提交总结；
4. commit body 必须写中文分条小结；
5. 用 git log -1 --format=fuller 确认 commit body 已写入。
```

---

## 3. 阶段判断标准

Phase 3 可以结束的标准：

- API sample set 至少 30 条，并有结构测试。
- parser prompt 和 smoke 检查能约束 MVP 六类 item。
- 真实 DeepSeek smoke 有记录。
- 移动端真实 API smoke 有记录，或明确记录设备/环境阻塞。
- `task_update` 规则文档存在，并且代码行为与文档一致。
- `task_update` 的 ready / needsSelection / noMatch UI 可用。
- 用户能看懂待确认卡片来源、保存目标和确认边界。
- 首页建议能说明为什么推荐。
- 自动保存可见、可修改、可撤销。
- ContextBuilder 至少支持 `current_suggestion` 和 `task_update_resolution`。
- 有统一检查脚本。
- 有真实试用 checklist。
- 有 Phase 3 验收文档。

Phase 3 不以“功能越多越好”为目标。真正目标是：用户可以连续试用几天，并且知道 App 记住了什么、为什么建议、如何撤销或删除。
