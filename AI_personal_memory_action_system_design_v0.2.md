# AI 个人效能助手：个人记忆与行动整理系统设计稿 v0.2

> 文档用途：供产品继续讨论，并可交给 Codex / Claude Code / 其他 AI 编程助手进行技术可行性评审。  
> 文档性质：这是 ChatGPT 基于当前讨论形成的产品与系统设计建议，不是最终方案。后续需要 Codex 从工程实现、数据结构、API 调用、Flutter/后端架构等角度继续审阅。  
> 当前重点：先确定产品底层逻辑，而不是先讨论 UI、商业化、推广话术或完整功能清单。  
> 讨论日期：2026-05-31

---

## 0. 当前结论概览

这个产品不应被理解为简单的：

- 任务管理工具
- 复盘工具
- 健康助手
- AI 伴侣
- 人生规划师
- To-do List

这些都只是表层能力。

更准确的底层定义是：

> 一个以自然语言为入口的个人记忆与行动整理系统。用户可以持续输入任务、想法、状态、经历、问题和背景信息，系统通过 AI 理解、结构化、确认、存储、检索和调用这些信息，从而越来越懂用户，并在合适的时候帮助用户整理任务、给出建议、辅助复盘和沉淀经验。

更简洁的产品表达可以是：

> 把脑子里的事说出来，AI 帮你整理好、记住重点，并告诉你下一步该做什么。

---

## 1. 与原始产品定义的区别

原始方案中的核心方向是“AI 个人效能助手”，包含任务管理、用户画像、自进化、下一步建议、复盘、提醒等模块。

本版设计并不是推翻原始方案，而是把它从“功能模块列表”升级为“信息流转系统”。

### 1.1 原始定义偏功能视角

原始方案更像是在说：

- 有任务系统
- 有复盘功能
- 有用户画像
- 有智能提醒
- 有下一步建议
- 有语音/文字输入

这个定义没有错，但容易让产品看起来像多个功能的组合。

### 1.2 本版定义偏底层机制视角

本版更关注：

- 用户说了一句话后，系统如何理解？
- 哪些信息需要回答？
- 哪些信息需要保存？
- 哪些信息需要转成任务？
- 哪些信息需要进入长期画像？
- 哪些信息只是短期状态？
- 哪些信息只是一次性事件？
- 以后用户再次提问时，系统应该调用哪些相关记忆？
- 如何避免每次把全部信息都上传给 DeepSeek？
- 如何避免画像乱记？
- 如何让 DeepSeek 输出能被 App 稳定写入数据库的结构化结果？

所以，本版设计的核心不是“做哪些功能”，而是：

> 自然语言输入 → AI 解析 → 结构化结果 → 用户确认 → 分层存储 → 相关记忆检索 → AI 建议/回答/复盘 → 记忆更新

---

## 2. 当前最重要的产品判断

### 2.1 这不是“大而全 AI 管家”，但容易滑向“大而全 AI 管家”

如果产品逻辑变成：

- 什么都能管
- 什么都要做
- 健康、任务、复盘、学习、会议、人生规划、AI 陪伴全部堆进去
- 每个模块都想做成专业工具

那它会变成“大而全 AI 管家”，这是危险的。

但如果产品核心始终是：

> 持续记录用户信息，理解用户状态，把自然语言转成结构化记忆与行动建议

那么它不是大而全，而是一个统一底层能力。

### 2.2 功能可以多，但底层逻辑必须统一

允许用户说各种内容：

- 我要明天联系客户
- 我今天太累了
- 我最近有点拖延
- 今天做菜糖放多了
- 我现在该干什么
- 帮我复盘这周
- 我最近睡眠不好
- 这个客户让我不太舒服，帮我分析一下
- 记一下我不喜欢太频繁的提醒

但系统不应该把这些全部当成同一种内容。

系统必须判断：

- 这是任务吗？
- 这是短期状态吗？
- 这是长期偏好吗？
- 这是生活经验吗？
- 这是复盘素材吗？
- 这是普通问答吗？
- 这是不该保存的闲聊吗？
- 这是敏感信息吗？
- 是否需要用户确认？

---

## 3. 总体系统架构

建议采用八层结构。

```text
1. 输入层
用户通过文字或系统语音输入法输入自然语言。

2. 理解层
调用 DeepSeek 判断用户输入中包含哪些意图和信息。

3. 结构化层
把自然语言解析成固定 JSON Schema，例如任务、短期状态、长期画像候选、复盘素材等。

4. 确认层
对需要写入长期数据库或影响任务系统的信息，展示给用户确认、修改或拒绝。

5. 存储层
把确认后的内容写入本地数据库 / 后端数据库 / 加密存储系统。

6. 检索层
用户后续提问时，根据当前问题检索相关任务、状态、画像和历史事件。

7. 推理层
把“当前问题 + 必要上下文”发送给 DeepSeek，生成回答、建议、复盘或更新候选。

8. 展示层
在首页、任务列表、画像页、历史记录页、对话区等位置展示结果。
```

---

## 4. 数据存储：哪些数据放哪里？

这是当前最重要的问题之一。

### 4.1 原始对话记录池

用户提到“原始对话记录池肯定上传到我们这一端，错误应该由厂家修正”。

这里需要谨慎区分两件事：

#### 4.1.1 产品系统可以保存原始记录

系统确实应该保存：

- 用户原始输入
- AI 解析结果
- 用户是否确认
- 最终写入了哪些模块
- 后续用户是否修改或删除

这对纠错非常重要。

#### 4.1.2 但“厂家人工修正用户数据”要非常谨慎

不建议默认由厂家人工查看、修正用户原始数据。

原因：

1. 用户数据高度敏感，可能包含健康、客户、工作、家庭、情绪等隐私信息。
2. 人工查看用户原文会显著增加隐私与合规风险。
3. 用户很难接受“厂家可以看到并修正我的个人记录”。
4. 如果未来产品做大，这会带来巨大的数据安全责任。

更合理的方式是：

> 厂家修正系统规则，不直接修正用户个人数据。

例如：

- 用户发现 AI 拆错任务，可以在 App 内自己改。
- 系统记录“这次解析失败”的匿名错误类型。
- 厂家根据大量匿名错误案例优化提示词、JSON Schema、解析规则和模型调用逻辑。
- 除非用户主动提交反馈并授权，否则厂家不应查看完整原文。

### 4.2 建议的数据保存策略

| 数据类型 | 是否保存 | 保存位置建议 | 是否需要用户可删除 |
|---|---|---|---|
| 原始输入文本 | 可以保存 | 本地优先，云端需用户授权 | 必须 |
| AI 解析结果 | 保存 | 本地 / 云端同步 | 必须 |
| 任务数据 | 保存 | 本地优先，可云同步 | 必须 |
| 长期画像 | 保存 | 本地优先，云端需加密 | 必须 |
| 短期状态 | 保存 | 本地优先 | 必须 |
| 复盘素材 | 保存 | 本地优先 | 必须 |
| 错误反馈 | 可保存 | 匿名化后上传 | 应允许关闭 |
| 模型调用日志 | 尽量少保存 | 服务端仅保留必要排障日志 | 应明确告知 |

---

## 5. “上传什么给 DeepSeek”的问题

用户提出的问题很关键：

> 每次调用 DeepSeek 时，到底上传什么？这是不是也得用 AI 判断？检索相关记忆用什么检索？

结论：

> 不能每次上传全部记忆，也不能完全依赖 AI 自由判断。最佳方案是“规则过滤 + 元数据筛选 + 向量检索 + LLM 重排”的混合方案。

早期 MVP 可以先简化，但架构上最好预留这个方向。

---

## 6. 为什么不能每次上传全部记忆？

短期看，用户背景信息不多，全部上传好像也可以。

但长期会遇到几个问题：

1. Token 变多，成本和延迟上升。
2. 无关信息太多，模型注意力被污染。
3. 敏感信息暴露面扩大。
4. 模型容易被旧信息误导。
5. 用户已经过期的状态可能还影响当前建议。
6. 后续很难解释“AI 为什么这么建议”。

所以应该遵循：

> 最小必要上下文原则：只上传当前请求真正需要的用户信息。

---

## 7. 记忆检索机制设计

### 7.1 不建议完全让 AI 检索

如果把所有历史记录都丢给 AI，让 AI 自己判断用哪些，会有问题：

- 成本高
- 速度慢
- 不稳定
- 隐私暴露更多
- 容易遗漏或误用
- 数据量大后不可扩展

### 7.2 推荐混合检索

建议分四层：

```text
用户当前输入
↓
规则判断意图
↓
按类型取必要数据
↓
关键词 / 标签 / 时间过滤
↓
向量检索相似记忆
↓
LLM 重排，选择最相关上下文
↓
组装 prompt
↓
调用 DeepSeek 生成结果
```

---

## 8. MVP 阶段可行的简化检索方案

早期不需要一开始就上复杂向量数据库，可以先用简单方案。

### 8.1 第一步：规则 + 类型过滤

系统先判断用户输入类型。

例如：

- “我现在该干什么” → 取任务 + 当前时间 + 短期状态 + 提醒偏好
- “帮我复盘这周” → 取本周任务 + 本周事件 + 本周状态
- “帮我分析这个客户为什么让我不舒服” → 取当前对话 + 相关情绪/沟通偏好，不默认取所有客户历史
- “我今天肩膀疼” → 记录短期健康状态，必要时加入健康观察池
- “记一下我不喜欢太频繁的提醒” → 长期偏好候选，需用户确认

### 8.2 第二步：时间范围过滤

不同请求默认取不同时间范围。

| 请求类型 | 默认检索范围 |
|---|---|
| 当前建议 | 今天 + 未来 7 天任务 + 最近 3 天状态 |
| 今日复盘 | 今天的任务、状态、事件 |
| 本周复盘 | 本周任务、状态、事件 |
| 项目/主题复盘 | 相关标签 + 最近 30/90 天 |
| 健康状态分析 | 最近 7/30 天相关健康记录 |
| 偏好判断 | 长期画像 + 最近相关事件 |
| 普通问答 | 不取私人记忆，除非用户问题明确关联自己 |

### 8.3 第三步：标签过滤

每条记录保存时就打标签。

例如：

- `task`
- `health`
- `work`
- `cooking`
- `customer`
- `emotion`
- `preference`
- `review_material`
- `short_term_state`
- `long_term_profile`

用户问做饭，就优先取 `cooking`。  
用户问工作，就优先取 `work`。  
用户问健康，就优先取 `health`。  
用户问“我现在该干嘛”，就优先取 `task` 和 `short_term_state`。

### 8.4 第四步：向量检索，后期加入

当数据量变大后，再加入 Embedding / 向量检索。

用途：

- 找到语义相似的历史记录
- 找到同类问题
- 找到相关生活经验
- 找到与当前项目相似的旧项目
- 找到过去类似状态下的处理方式

### 8.5 第五步：LLM 重排

检索出来 10-30 条候选记忆后，再让 DeepSeek 或更便宜的模型判断：

> 哪些记忆与当前问题最相关？

最终只带 3-8 条进入正式 prompt。

---

## 9. 上下文组装策略

### 9.1 当前建议场景

用户问：

> 我现在该干什么？

建议传给 DeepSeek：

```text
- 当前时间
- 今日任务列表
- 未来 7 天重要任务
- 已逾期任务
- 最近 3 天短期状态
- 用户提醒偏好
- 用户工作/生活节奏相关长期画像
- 用户刚刚输入的问题
```

不建议传：

```text
- 全部历史对话
- 全部长期画像
- 很久以前的做饭记录
- 无关健康信息
- 无关客户信息
```

### 9.2 复盘场景

用户问：

> 帮我复盘这周。

建议传：

```text
- 本周任务：完成 / 延期 / 取消 / 未完成
- 本周短期状态记录
- 本周重要事件
- 本周用户主动记录的复盘素材
- 用户长期目标或偏好中与复盘有关的部分
```

### 9.3 生活经验场景

用户说：

> 今天做菜糖放多了，有点太甜。

建议传：

```text
- 当前输入
- 最近同类 cooking 记录
- 用户饮食偏好，如果有
```

AI 可以回复：

```text
我可以帮你记成一条做菜经验：这次这道菜糖放多了，成品偏甜。你还记得大概放了多少糖吗？如果你愿意，我可以记录为“下次同类菜减少糖量”。
```

### 9.4 客户/项目分析场景

用户说：

> 最近跟某个客户聊得不太愉快，你帮我分析一下。

注意：多数用户不会完整上传客户全量信息，也不会把项目文件都交给这个 App。

因此系统不应假设自己知道全部项目背景。

建议方式：

```text
- 只基于用户当前描述分析
- 如信息不足，温和追问关键缺口
- 不强行调用无关历史
- 不假装知道客户全貌
```

AI 回复风格：

```text
我先基于你刚才说的情况分析，不假设我知道完整背景。你可以补充三个点：对方具体哪句话让你不舒服、你当时怎么回应、这件事是否影响后续合作。
```

---

## 10. 信息分类系统

这是系统的核心。

DeepSeek 每次解析用户输入时，需要判断其中包含哪些信息类型。

建议第一版信息类型如下。

### 10.1 普通问答 `general_answer`

用户只是问知识、问建议、问解释，不需要保存。

示例：

- Flutter 是什么？
- 牛排怎么煎？
- DeepSeek API 怎么用？
- 什么是显著性分析？

处理方式：

- 直接回答
- 默认不写入用户记忆
- 除非用户明确说“记住这个”

### 10.2 新建任务 `task_create`

用户表达了未来要做的事。

示例：

- 明天上午联系王总
- 这周整理项目方案
- 晚上记得买牛奶

处理方式：

- 解析任务标题、时间、优先级、项目、提醒需求
- 需要用户确认后写入任务表

### 10.3 更新任务 `task_update`

用户修改已有任务。

示例：

- 把联系王总改到后天
- 那个方案不用今天做了
- 把买牛奶标记完成

处理方式：

- 匹配已有任务
- 如果匹配不确定，询问用户
- 用户确认后更新任务

### 10.4 完成任务 `task_complete`

用户表示某事已经完成。

示例：

- 王总已经联系完了
- 方案我写好了
- 今天健身完成了

处理方式：

- 匹配任务
- 标记完成
- 可询问是否记录完成感受或复盘素材

### 10.5 取消任务 `task_cancel`

用户表示不做了。

示例：

- 这个会不去了
- 这个方案不用做了
- 今天不健身了

处理方式：

- 标记取消
- 可选记录取消原因
- 不直接进入长期画像
- 可作为短期复盘素材

### 10.6 延期任务 `task_delay`

用户表示推迟。

示例：

- 这个改到明天
- 方案下周再写
- 健身今天不做了，明天补

处理方式：

- 更新任务时间
- 记录延期原因
- 可作为复盘素材

### 10.7 短期状态 `short_term_state`

用户表达当前或近期状态。

示例：

- 我今天很累
- 这两天心情不错
- 最近睡眠不好
- 今天肩膀疼
- 我现在在路上，不方便打电话

处理方式：

- 保存为短期状态
- 设置有效期
- 默认不进入长期画像
- 健康相关状态需要更谨慎

### 10.8 长期画像候选 `long_term_profile_candidate`

用户表达比较稳定的身份、偏好、习惯、目标。

示例：

- 我不喜欢太频繁的提醒
- 我一般晚上效率比较高
- 我经常需要出差
- 我现在主要做项目现场相关工作
- 我更喜欢直接一点的建议

处理方式：

- 作为长期画像候选
- 建议用户确认
- 确认后进入长期画像
- 可允许用户修改措辞

### 10.9 生活经验 / 事件记录 `life_event`

用户记录一次经历，但不一定进入画像。

示例：

- 今天做菜糖放多了
- 今天跟客户沟通不太舒服
- 今天运动后膝盖有点不适
- 今天临时改了行程

处理方式：

- 保存为事件记录
- 可打标签
- 可作为后续复盘素材
- 不默认进入长期画像

### 10.10 复盘请求 `review_request`

用户明确要求复盘。

示例：

- 帮我复盘今天
- 复盘一下这周
- 帮我分析最近为什么拖延
- 复盘一下我最近健身情况

处理方式：

- 判断复盘范围：时间 / 主题 / 项目 / 状态
- 检索相关任务、状态、事件
- 生成短复盘
- 可保存复盘结果

### 10.11 建议请求 `action_suggestion_request`

用户问现在做什么或下一步怎么做。

示例：

- 我现在该干嘛？
- 今天先做什么？
- 接下来怎么安排？
- 这几个任务哪个优先？

处理方式：

- 调用任务、时间、状态、画像
- 给出 1-3 个建议
- 解释原因
- 避免命令式口吻

### 10.12 记忆删除 / 修改请求 `memory_edit_request`

用户要求修改或删除记忆。

示例：

- 别记这个了
- 把我不喜欢开会那条删掉
- 我不是不喜欢开会，我是不喜欢无效会议
- 清空今天的状态

处理方式：

- 匹配相关记忆
- 展示给用户确认
- 修改或删除
- 保留必要的操作日志，但不保留敏感原文

### 10.13 敏感信息 `sensitive_info`

涉及健康、客户、财务、身份、家庭、情绪等敏感内容。

处理方式：

- 默认谨慎
- 明确告诉用户可删除
- 不轻易进入长期画像
- 不做医疗、法律、财务等高风险结论
- 需要时提醒用户寻求专业人士帮助

### 10.14 无需保存闲聊 `ephemeral_chat`

用户只是闲聊、吐槽、过渡表达。

示例：

- 哈哈
- 我随便说说
- 今天真烦
- 算了没事

处理方式：

- 可以回应
- 默认不保存
- 除非连续出现并对状态判断有价值

---

## 11. 记忆分层系统

建议分成六层。

### 11.1 L0：当前会话上下文

只在当前对话中有效。

示例：

- 用户刚才提到的某个事情
- 当前问题的上下文
- 刚刚还没确认的任务解析结果

保存时间：

- 当前会话内
- 可在会话结束后只保留摘要或原始记录

### 11.2 L1：短期状态

保存今天到最近几天的状态。

示例：

- 今天很累
- 这两天心情好
- 最近睡眠不好
- 今天不方便打电话

建议有效期：

| 状态类型 | 默认有效期 |
|---|---|
| 今日情绪/精力 | 1 天 |
| 近期忙碌状态 | 3-7 天 |
| 短期身体不适 | 3-7 天，重复出现则延长 |
| 当前场景限制 | 几小时到 1 天 |

处理方式：

- 可自动记录
- 展示在“近期状态”中
- 用户可删除
- 过期后降权或归档，不再主动影响建议

### 11.3 L2：任务与行动记录

保存具体行动。

示例：

- 联系客户
- 整理方案
- 健身
- 买东西
- 复盘某件事

保存时间：

- 完成、取消、延期后仍可保留
- 用于复盘
- 用户可删除或归档

注意：

- 取消和延期是重要信息，但不等于长期画像。
- 一次取消不代表用户习惯拖延。
- 多次同类取消才可能形成“行为模式候选”。

### 11.4 L3：事件与复盘素材

保存用户经历和经验。

示例：

- 今天做菜糖放多了
- 今天跟客户沟通不愉快
- 今天运动后膝盖不舒服
- 今天临时取消了某安排

保存时间建议：

| 事件类型 | 保存策略 |
|---|---|
| 普通生活小事 | 默认 30-90 天，可归档 |
| 用户主动要求记住的经验 | 长期保存 |
| 与任务/目标有关事件 | 保留到相关目标结束 |
| 健康异常事件 | 谨慎保存，可较长保留，但不下诊断 |
| 情绪吐槽 | 默认短期，除非重复出现 |

### 11.5 L4：长期画像

保存相对稳定的信息。

示例：

- 用户职业/身份
- 长期目标
- 偏好
- 工作节奏
- 沟通风格偏好
- 提醒偏好
- 常见任务类型

写入原则：

- 需要较高置信度
- 最好用户确认
- 不从一次吐槽直接推断人格
- 保留来源与更新时间
- 允许用户修改

### 11.6 L5：行为模式

从长期数据中总结出的规律。

示例：

- 用户经常在晚上处理文字工作
- 用户经常延期低优先级生活任务
- 用户周三任务完成率较低
- 用户在出差期间不适合安排复杂写作任务

写入原则：

- 需要多次证据
- 不要道德化评价用户
- 使用中性语言
- 可显示“基于最近 N 次记录推断”
- 用户可否认或删除

---

## 12. 记忆写入规则

### 12.1 基本原则

1. 用户明确要求记住的，优先保存。
2. 用户明确要求删除的，必须删除或停止使用。
3. 单次情绪表达不进入长期画像。
4. 单次失败不进入行为模式。
5. 健康信息只记录事实，不做诊断。
6. 长期画像尽量让用户确认。
7. 短期状态可以自动记录，但必须可见、可删。
8. 任务写入必须让用户确认，除非用户开启自动写入模式。
9. 敏感信息默认谨慎处理。
10. 所有 AI 推断都应保留来源和置信度。

### 12.2 置信度设计

建议每条记忆都有置信度：

| 置信度 | 含义 | 示例 |
|---|---|---|
| high | 用户明确表达并确认 | “我不喜欢频繁提醒” |
| medium | 用户多次表达但未确认 | 多次说晚上效率高 |
| low | AI 从一次表达中推测 | “用户可能最近压力较大” |

### 12.3 记忆来源

每条记忆建议保存来源：

- 用户直接陈述
- AI 从任务记录总结
- AI 从复盘中总结
- 用户手动编辑
- 系统统计推断

### 12.4 记忆状态

建议每条记忆有状态：

- active：正在使用
- candidate：候选，等待确认
- expired：已过期
- archived：归档，不主动调用
- rejected：用户否认
- deleted：已删除

---

## 13. DeepSeek 输出格式设计

这是后续交给 Codex 评审的重点。

目标：

> DeepSeek 不只是回答用户，而是输出一份可被 App 读取、展示、确认、写库的结构化结果。

### 13.1 总体 JSON Schema 示例

```json
{
  "user_reply": "我帮你整理出了 2 个任务，并记录了你今天有些疲惫。你可以确认或修改。",
  "input_summary": "用户提到明天联系王总、这周整理方案，并表示今天很累。",
  "intent_types": ["task_create", "short_term_state"],
  "items": [
    {
      "type": "task_create",
      "title": "联系王总",
      "description": null,
      "due_time_text": "明天上午",
      "due_time_iso": null,
      "priority": "medium",
      "project": null,
      "tags": ["work", "customer"],
      "need_user_confirm": true,
      "confidence": 0.86,
      "source_text": "明天上午联系王总"
    },
    {
      "type": "task_create",
      "title": "整理项目方案",
      "description": null,
      "due_time_text": "这周",
      "due_time_iso": null,
      "priority": "medium",
      "project": null,
      "tags": ["work"],
      "need_user_confirm": true,
      "confidence": 0.78,
      "source_text": "这周整理项目方案"
    },
    {
      "type": "short_term_state",
      "content": "用户今天感觉疲惫",
      "valid_until_text": "今天结束",
      "valid_days": 1,
      "tags": ["energy"],
      "need_user_confirm": false,
      "confidence": 0.72,
      "source_text": "我今天很累"
    }
  ],
  "memory_candidates": [],
  "safety_flags": [],
  "need_follow_up_question": false,
  "follow_up_question": null
}
```

### 13.2 为什么要 JSON？

因为 App 不能依靠自然语言回答来写数据库。

错误方式：

```text
AI：好的，我帮你记下来了，明天联系王总。
```

这种回答人能看懂，但程序不好稳定处理。

正确方式：

```json
{
  "type": "task_create",
  "title": "联系王总",
  "due_time_text": "明天",
  "need_user_confirm": true
}
```

App 可以根据 type 写入任务表，根据 need_user_confirm 决定是否弹出确认卡片。

### 13.3 需要测试的关键点

后续开发时需要测试：

- DeepSeek 是否稳定输出合法 JSON
- 是否会漏掉任务
- 是否会把闲聊误判成记忆
- 是否会过度推断长期画像
- 是否能正确识别取消/延期/完成
- 是否能处理一句话里多个意图
- 是否能在时间模糊时保留原文，而不是乱填日期
- 是否能输出 confidence
- 是否能区分短期状态和长期画像

---

## 14. 用户确认机制

### 14.1 必须确认的内容

- 新建任务
- 修改任务时间
- 删除任务
- 长期画像写入
- 用户偏好写入
- 重要健康信息长期保存
- 行为模式总结

### 14.2 可以自动记录但必须可见的内容

- 今日状态
- 短期情绪
- 临时场景
- 普通事件
- 复盘素材候选

### 14.3 默认不保存的内容

- 普通知识问答
- 闲聊
- 单次情绪化表达
- 不明确的猜测
- 没有后续价值的碎片

### 14.4 推荐交互

用户输入后，AI 回复不只是文字，而是卡片：

```text
我理解到以下内容：

[任务] 明天上午联系王总
[任务] 本周整理项目方案
[状态] 今天比较疲惫，有效期：今天

按钮：
- 全部确认
- 修改
- 不保存
```

---

## 15. 首页展示建议

用户目前接受：

- 用户主动问，AI 给建议
- 任务列表顶部可以半主动展示当前建议
- 系统通知默认关闭
- 用户可自行开启通知频率

建议首页结构：

```text
顶部：当前建议卡片
- 现在更适合先做什么
- 为什么建议做它
- 预计耗时
- 可操作按钮：开始 / 换一个 / 延期 / 忽略

中部：今日任务
- 待办
- 已完成
- 延期
- 取消

下部：近期状态
- 今天精力偏低
- 最近在忙某项目
- 最近睡眠不好

底部：万能输入框
- 说出任务、状态、想法或问题
```

系统通知策略：

| 提醒类型 | 默认状态 |
|---|---|
| 首页建议 | 开启 |
| App 内提醒 | 开启 |
| 系统推送通知 | 默认关闭 |
| 高频催促 | 必须用户主动开启 |
| 健康/风险类提醒 | 单独授权 |

---

## 16. 复盘能力设计

用户不希望做成复杂会议复盘工具，也不认为多数用户会完整上传项目文件或客户全量背景。

因此复盘不应重做成“会议纪要/项目管理系统”。

### 16.1 复盘定位

复盘应定位为：

> 基于用户自己记录过的任务、状态、事件和经验，帮助用户进行个人行动与生活经验的总结。

### 16.2 不建议重点做的复盘

- 专业会议纪要复盘
- 企业项目全流程复盘
- 客户全量 CRM 分析
- 文档级深度复盘

这些可以由其他工具完成。

### 16.3 建议重点做的复盘

- 今日复盘
- 本周复盘
- 某个主题复盘
- 某个习惯复盘
- 某段状态复盘
- 某次生活经验复盘
- 任务完成/延期/取消原因分析

### 16.4 复盘输出风格

默认短，不写长篇作文。

推荐结构：

```text
1. 你完成了什么
2. 卡住/延期/取消了什么
3. 可能的原因
4. 下次可以怎么改
5. 需要我帮你记住的一条经验
```

如果用户要求详细分析，再展开。

---

## 17. 客户/项目类分析的边界

用户指出：

多数用户不会持续详细记录某个客户的所有资料，也不会把完整项目文件都交给这个 App。

因此系统不能假装自己知道完整背景。

### 17.1 正确策略

当用户说：

> 最近跟某个客户不太愉快，你帮我分析一下。

AI 应该：

- 基于用户当前提供的信息分析
- 明确说明“我先基于你提供的情况判断”
- 如信息不足，追问关键问题
- 不默认调用无关历史
- 不假装了解整个客户关系
- 不强行让用户录入完整 CRM 信息

### 17.2 可追问的问题

例如：

```text
我先基于你刚才说的情况分析。为了更准确，我需要知道：
1. 对方具体哪句话或哪件事让你不舒服？
2. 你当时是怎么回应的？
3. 这件事是否会影响后续合作？
```

这种方式比假装“全知”更可信。

---

## 18. 隐私与合规原则

这是高优先级问题。

### 18.1 核心风险

产品会涉及：

- 工作信息
- 客户信息
- 健康信息
- 情绪状态
- 家庭生活
- 行程安排
- 个人习惯
- 长期画像

这些都高度敏感。

### 18.2 原则

1. 本地优先。
2. 云端同步必须用户授权。
3. 上传给 DeepSeek 遵循最小必要原则。
4. 用户可查看 AI 对自己的理解。
5. 用户可修改、删除、清空数据。
6. 厂家默认不人工查看用户原文。
7. 错误反馈应尽量匿名化。
8. 健康、法律、财务等敏感领域不做高风险结论。
9. 隐私协议必须清晰说明数据如何被使用。
10. 后期如果产品做大，需要专业安全团队和法律合规支持。

---

## 19. MVP 建议

虽然长期功能可以很丰富，但 MVP 不建议一次做太大。

### 19.1 MVP 核心目标

验证一个问题：

> 用户是否愿意把脑子里的事情说给这个 App，并接受它帮自己整理成任务、状态和建议？

### 19.2 MVP 必做

1. 万能输入框
2. DeepSeek 解析自然语言为 JSON
3. 任务创建/确认/完成/取消/延期
4. 短期状态记录
5. 简单长期画像候选
6. 首页当前建议
7. 简单复盘
8. 用户可查看和修改“AI 对我的理解”

### 19.3 MVP 暂不重点做

- 自建语音识别
- 语音回复
- 复杂健康管理
- 专业会议复盘
- 完整 CRM
- 复杂项目管理
- 订阅系统
- 多语言
- 高级向量检索
- 云端多端同步

---

## 20. 需要交给 Codex / Claude 进一步评审的问题

以下内容需要后续让 Codex 或 Claude 从工程角度继续审阅。

### 20.1 DeepSeek 结构化输出是否稳定

需要评审：

- 如何强制输出 JSON
- 如何做 JSON 校验
- 模型输出异常时如何兜底
- 是否需要 function calling / tool calling 风格
- 是否需要二次解析
- 是否需要低温度参数
- 是否需要 schema validator

### 20.2 数据库表结构

需要设计：

- tasks
- memories
- short_term_states
- long_term_profiles
- events
- review_records
- conversations
- ai_parse_results
- user_confirmations
- reminders

### 20.3 检索系统

需要评审：

- MVP 是否先用 SQLite 查询 + 标签 + 时间过滤
- 后续是否加入 embedding
- 如果加入 embedding，用本地向量库还是服务端向量库
- 如何做相关性排序
- 如何控制传给 DeepSeek 的上下文长度

### 20.4 隐私架构

需要评审：

- 本地存储如何加密
- 服务端是否保存原始文本
- API Key 如何保护
- 是否需要后端中转 DeepSeek
- 日志如何脱敏
- 用户删除如何真正生效
- 是否需要端到端加密

### 20.5 任务解析与确认流程

需要评审：

- 一句话多个任务如何拆
- 时间解析如何处理
- 模糊时间如何保留
- 任务匹配如何做
- 用户修改后如何回写
- AI 解析错误如何记录

---

## 21. 当前最应该继续深入讨论的 5 个问题

不建议下一轮继续泛泛讨论“用户是谁”或“商业模式”。

更应该优先讨论：

1. 信息分类系统是否完整  
   也就是用户输入到底可能分成哪些类型。

2. 记忆分层系统是否合理  
   长期画像、短期状态、事件、任务、复盘素材如何区分。

3. DeepSeek 输出 JSON Schema  
   这是从产品想法进入开发的关键。

4. 记忆检索与上下文组装  
   每次调用模型时到底带哪些信息。

5. 用户确认与纠错机制  
   哪些自动保存，哪些必须确认，错了怎么改。

---

## 22. 当前最佳一句话总结

这个产品的灵魂不是“任务管理”或“复盘”，而是：

> 这句话对用户的未来有没有价值？  
> 如果有，应该保存到哪里？  
> 保存多久？  
> 以后什么时候拿出来用？  
> 再次调用 AI 时，应该带哪部分记忆，而不是带全部记忆？

只要围绕这个问题设计，产品就不会变成大而全 AI 管家。

---

## 23. 给后续开发 AI 的提示

请 Codex / Claude 重点评价：

1. 以上架构是否适合 Flutter + 本地数据库 + DeepSeek API。
2. DeepSeek 输出 JSON 并写入不同数据表的方案是否可行。
3. MVP 阶段是否可以不用向量数据库，只用标签、时间、类型过滤。
4. 如何设计数据库 schema。
5. 如何设计 prompt，使 DeepSeek 稳定输出结构化结果。
6. 如何在用户确认后写入数据库。
7. 如何设计最小可用的记忆检索系统。
8. 哪些地方会导致代码复杂度过高。
9. 哪些设计需要后端，哪些可以先纯本地实现。
10. 隐私与数据安全上有哪些明显漏洞。

---

## 24. 版本说明

本文件是 v0.2 讨论稿。

它不是最终产品方案，而是为了把当前想法从“功能愿景”推进到“系统设计”。

后续建议继续生成：

- v0.3：信息分类系统详细版
- v0.4：记忆分层与写入规则详细版
- v0.5：DeepSeek JSON Schema 与 prompt 设计
- v0.6：数据库表结构草案
- v0.7：Flutter MVP 页面与流程设计
