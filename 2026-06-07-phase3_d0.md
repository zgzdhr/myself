可以，我先把你刚才这段想法吸收进这次 D0 讨论里。你说的几个点都很关键，而且我认为你判断是对的：**不能继续依赖关键词和有限时间词**，因为真实用户的表达一定是生活化、口语化、跳跃式的。现在必须把规则从“关键词匹配”升级为“语义判断 + 样例校准 + 用户可修正”。

下面我分两个模块说。

---

# 模块一：分析你刚才说的想法

## 1. `task_update` 可以自动保存，我同意

你说：

> 完成任务、取消任务，也可以自动保存，但不要直接从任务栏删掉。

这个判断是对的。

之前我偏保守，认为 `task_update` 不自动保存，是为了避免误操作。但你这个方案更适合真实产品：

```text
允许自动应用 task_update
但不要物理删除 / 不要完全消失
而是变成“可见的状态变化”
用户可以发现错误并撤销或修正
```

这比“不自动处理”更实用。

因为用户说：

```text
王总那个事做完了
明天会议取消
客户沟通推到后天
```

这些本来就是很明确的行动更新。如果每次都必须确认，用户会觉得麻烦。

所以我建议改成：

| 类型                     | 是否可自动保存 | 保存方式              |
| ---------------------- | ------- | ----------------- |
| `task_update.complete` | 可以自动应用  | 任务仍显示，但划线，标注“已完成” |
| `task_update.cancel`   | 可以自动应用  | 任务仍显示，但划线，标注“已取消” |
| `task_update.delay`    | 谨慎自动应用  | 更新日期，同时显示“已延期”    |
| `task_update.edit`     | 谨慎自动应用  | 改标题/内容，但保留修改痕迹    |
| 低置信度 / 多候选 / 无目标       | 不自动应用   | 进入待确认或让用户选择       |

---

## 2. 不应该把完成/取消任务直接删掉

你说的“划线 + 标注状态”非常重要。

现在代码里 `cancel` 会调用 `markTaskDeleted`，`complete` 会调用 `markTaskArchived`。也就是说，完成或取消后，任务会从 active 任务里被排除。这个逻辑在技术上简单，但产品上有风险。因为一旦 AI 识别错，用户在今日任务里可能看不到它了，反而更难发现错误。当前代码里的 task update 应用逻辑确实是 complete → archived，cancel → deleted。

你提出的方式更好：

```text
任务完成 / 取消后，仍然在今日任务区域可见
但用划线、灰色、状态标签展示
用户可以撤销
```

这对应的数据设计上，我建议不要再只用 `RecordStatus.archived/deleted` 表示任务业务状态，而是逐步引入：

```text
record_status：记录是否有效，例如 confirmed / deleted / archived
task_status：任务业务状态，例如 active / completed / cancelled
```

短期内如果不想改表，可以先用现有 status 过渡；但中期应该加 `task_status`。否则“删除记录”和“取消任务”会混在一起。

---

## 3. `task_update` 自动保存必须有“可撤销机制”

你这个产品要允许 AI 自动整理，但不能让 AI 偷偷改掉用户数据。

所以规则应该是：

```text
AI 可以自动应用高置信 task_update
但所有自动应用必须：
1. 可见
2. 可撤销
3. 有来源原文
4. 有状态标注
5. 不直接物理删除
```

比如用户说：

```text
王总那个事做完了
```

系统应该显示：

```text
✓ 联系王总     已完成
来源：王总那个事做完了
[撤销]
```

如果 AI 识别错，用户马上能点撤销。

---

## 4. 规则表需要更多例句，是的，而且非常需要

你问是不是需要更大量的举例。

我的判断是：**需要，而且这是 D0 最重要的工作。**

因为 Flash 这类模型不是不能理解，而是你必须告诉它：

```text
你的产品里，“记忆价值”和“任务价值”怎么定义。
```

普通模型看到：

```text
晚上同学约我去网吧，我同意了
```

它可能从“发生了一次社交事件”的角度理解成 `life_event`。

但你的产品语义里，它应该理解成：

```text
未来安排 + 用户已同意 + 需要用户行动
= task_create
```

所以必须用大量样例把边界钉死。

而且这些样例不是随便堆数量，而是要覆盖边界：

```text
未来安排 vs 已发生事件
当前状态 vs 长期画像
一次经验 vs 稳定偏好
任务创建 vs 任务更新
普通问答 vs 可保存记忆
自动保存 vs 必须确认
```

现在 sample set 已经有任务、任务更新、短期状态、生活事件、普通问答、长期画像、多意图和边界样例，但还缺少“社交约定、娱乐安排、别人邀请我我同意、临时答应、生活预约”这类真实中文表达。

---

## 5. “关键词 + 有限时间词”必须被降级为兜底，而不是主逻辑

你说：

> 系统依赖关键词和有限时间词一定是错误的。

我同意。

现在自动保存 `task_create` 主要依赖 `_inferTaskDue` 是否能推断出时间，而 `_inferTaskDue` 只支持“明天上午、明天下午、明天、今天”。

这在真实输入里肯定不够。

比如这些都可能是任务：

```text
晚上同学约我去网吧，我同意了
过会儿提醒我给客户回电话
下周找个时间去猪场看一下
这两天把合同整理出来
月底前把方案发给张总
等我到酒店后给王总回消息
```

这些不一定能被当前 `_inferTaskDue` 识别，但语义上明显是任务。

所以后面应该改成：

```text
AI parser 负责语义分类和时间理解
本地 _inferTaskDue 只做兜底
不能让本地关键词决定“是不是任务”
```

也就是说：

| 层级             | 作用                    |
| -------------- | --------------------- |
| Flash / parser | 判断语义类型：任务、状态、事件、画像、问答 |
| sample set     | 约束 Flash 的判断边界        |
| prompt         | 告诉 Flash 分类原则         |
| 本地规则           | 做安全兜底，不做主要语义判断        |
| UI             | 允许用户修正 AI 的判断         |

---

## 6. 这些修改应该放在哪里？

我建议拆成三个位置。

### 位置一：D0 文档层

先形成：

```text
docs/architecture/chinese-semantic-classification-rules.md
```

这里写清楚：

```text
什么是任务
什么是任务更新
什么是短期状态
什么是生活事件
什么是长期画像
什么是普通问答
哪些可以自动保存
哪些必须确认
哪些不能保存
```

这是产品规则源头。

---

### 位置二：API parser 层

修改：

```text
apps/api/src/services/parserPrompt.ts
apps/api/src/services/parserSampleSet.ts
apps/api/scripts/smokeParseSamples.ts
```

重点是让 Flash 学会：

```text
未来约定 / 答应别人 / 已安排 / 要去做
优先 task_create，而不是 life_event
```

当前 prompt 已经有一些规则，比如短期状态、长期画像、一次性事件、普通问答的边界，但还缺“未来社交/生活安排也是任务”的规则。

---

### 位置三：移动端任务状态层

修改：

```text
apps/mobile/lib/features/extracted_items/extracted_items_controller.dart
apps/mobile/lib/data/local_db/tables.dart
apps/mobile/lib/features/home/
apps/mobile/lib/features/memory/
```

这里要处理：

```text
task_update 自动应用
完成/取消任务不直接消失
任务划线显示
状态标签
撤销
```

这个不一定马上做，但要记入后续任务。

---

# 模块二：D0 中文语义分类规则初稿

下面是我们一起完成 D0 的第一版。它可以作为后续文档的基础。

---

# D0：中文语义分类规则

## 总原则

这个 App 的分类不是按关键词分，而是按这句话的“未来使用价值”分。

核心问题是：

```text
这句话未来应该怎么被用？
```

如果它未来要提醒用户做事，就是任务。
如果它描述用户当前/近期状态，就是短期状态。
如果它记录一次经历、教训、事实，就是生活事件。
如果它表达长期稳定偏好/习惯/背景，就是长期画像候选。
如果它只是问问题、聊天、求建议，就是普通问答。
如果它是在改变已有任务，就是任务更新。

---

# 1. `task_create`：新建任务

## 白话定义

用户表达了一个未来要做的事情、已经答应的安排、需要处理的行动、要准备的事项，就属于 `task_create`。

重点不是有没有“提醒我”“记得”这些关键词，而是：

```text
未来 + 用户需要行动
```

## 判断条件

符合以下任意一种，就优先判断为 `task_create`：

1. 用户要做某事。
2. 用户答应了某个安排。
3. 有人约用户，用户同意了。
4. 未来某个时间要发生，且用户需要参与。
5. 用户需要准备、联系、发送、整理、拜访、处理。
6. 用户说“别忘了”“记得”“到时候”“回头”“找时间”。
7. 即使是娱乐、社交、生活，也只要是未来安排，就是任务。

## 真实例句

| 例句             | 分类            | 自动保存建议       | 说明                 |
| -------------- | ------------- | ------------ | ------------------ |
| 明天上午联系王总       | `task_create` | 可自动保存        | 明确未来行动             |
| 晚上同学约我去网吧，我同意了 | `task_create` | 可自动保存        | 未来社交安排             |
| 下周一去上海出差       | `task_create` | 可自动保存        | 未来行程               |
| 这两天找时间整理客户资料   | `task_create` | 可自动保存，但时间模糊  | 有行动，但时间不精确         |
| 回头把会议纪要发给团队    | `task_create` | 可自动保存，时间模糊   | 明确任务               |
| 到酒店后给王总回电话     | `task_create` | 可自动保存，条件触发时间 | 条件型任务              |
| 周五之前把预算表看完     | `task_create` | 可自动保存        | 有截止时间              |
| 过会儿提醒我给客户发消息   | `task_create` | 可自动保存        | 明确提醒任务             |
| 晚上和同学吃饭        | `task_create` | 可自动保存        | 未来约定，不是 life_event |
| 明天去猪场看一下正骨项目   | `task_create` | 可自动保存        | 业务行动               |

## 容易混淆边界

| 句子             | 正确分类                               | 不应分类为         | 原因             |
| -------------- | ---------------------------------- | ------------- | -------------- |
| 晚上同学约我去网吧，我同意了 | `task_create`                      | `life_event`  | 还没发生，是未来安排     |
| 今天同学约我去网吧，我去了  | `life_event`                       | `task_create` | 已发生            |
| 我现在在网吧         | `short_term_state`                 | `life_event`  | 当前状态/位置        |
| 我以后少去网吧        | `profile_candidate` 或 `life_event` | `task_create` | 表达偏好/经验，不是具体任务 |
| 网吧适合学习吗？       | `general_answer`                   | `task_create` | 普通问答           |

## 保存策略

建议：

```text
明确未来安排：自动保存
时间模糊但行动明确：自动保存，但标注时间模糊
不确定是否要行动：进入待确认
```

---

# 2. `task_update`：任务更新

## 白话定义

用户不是在创建新任务，而是在改变一个已经存在的任务。

包括：

```text
完成
取消
延期
改内容
改时间
改对象
```

## 判断条件

符合以下情况，判断为 `task_update`：

1. 用户说某事做完了。
2. 用户说取消某事。
3. 用户说推迟、提前、改到另一个时间。
4. 用户说把 A 改成 B。
5. 用户说某个任务不用做了。
6. 用户说“那个事”“这件事”并带有完成/取消/延期动作。

## 真实例句

| 例句           | 分类            | 自动保存建议     | 说明        |
| ------------ | ------------- | ---------- | --------- |
| 王总那个事做完了     | `task_update` | 高置信匹配时自动应用 | 完成任务      |
| 联系王总这件事已经完成了 | `task_update` | 可自动应用      | 目标明确      |
| 明天会议取消       | `task_update` | 可自动应用但保留可见 | 取消任务      |
| 把客户资料整理往后推三天 | `task_update` | 目标明确时自动应用  | 延期任务      |
| 把联系王总改成联系张总  | `task_update` | 谨慎自动应用     | 修改任务内容    |
| 晚上去网吧这个事取消   | `task_update` | 可自动应用      | 取消未来安排    |
| 明天客户沟通推到后天   | `task_update` | 可自动应用      | 延期        |
| 那个事终于搞定了     | `task_update` | 不自动应用      | 目标模糊，需要选择 |

## 自动保存策略

我建议改成：

```text
task_update 可以自动保存，但要分级。
```

| 情况           | 处理              |
| ------------ | --------------- |
| 唯一高置信候选      | 自动应用            |
| 多个候选         | 让用户选择           |
| 无候选          | 不应用，提示没找到       |
| 目标是“那个事/这个事” | 不自动应用，除非上下文唯一   |
| 完成/取消        | 不从任务栏消失，显示划线和状态 |
| 延期/编辑        | 保留修改前后痕迹        |
| 所有自动应用       | 必须可撤销           |

## UI 建议

完成任务：

```text
~~联系王总~~   已完成
来源：王总那个事做完了
[撤销]
```

取消任务：

```text
~~晚上去网吧~~   已取消
来源：晚上去网吧这个事取消
[撤销]
```

延期任务：

```text
客户沟通   已延期到后天
来源：明天客户沟通推到后天
[撤销]
```

---

# 3. `short_term_state`：短期状态

## 白话定义

用户当前、今天、最近几天的临时状态。

它不是任务，不是长期画像，而是会影响当前建议的上下文。

## 判断条件

符合以下情况，判断为 `short_term_state`：

1. 今天很累。
2. 最近睡眠不好。
3. 当前在路上。
4. 现在不方便打电话。
5. 这几天心情不好。
6. 今天状态很好。
7. 最近事情有点乱。
8. 临时身体、精力、情绪、位置状态。

## 真实例句

| 例句            | 分类                 | 自动保存建议 | 说明     |
| ------------- | ------------------ | ------ | ------ |
| 我今天很累         | `short_term_state` | 自动保存   | 今日状态   |
| 这两天睡眠不太好      | `short_term_state` | 自动保存   | 近期状态   |
| 我现在在路上，不方便接电话 | `short_term_state` | 自动保存   | 当前场景   |
| 最近状态不太好       | `short_term_state` | 自动保存   | 近期状态   |
| 今天心情不错        | `short_term_state` | 自动保存   | 临时情绪   |
| 我下午可能不适合处理重任务 | `short_term_state` | 自动保存   | 当前精力约束 |
| 最近手上项目有点乱     | `short_term_state` | 自动保存   | 当前工作状态 |

## 容易混淆边界

| 句子         | 正确分类                              | 不应分类为               | 原因          |
| ---------- | --------------------------------- | ------------------- | ----------- |
| 我今天很累      | `short_term_state`                | `profile_candidate` | 单次状态不是长期画像  |
| 我一直不适合早上开会 | `profile_candidate`               | `short_term_state`  | 稳定偏好        |
| 今天客户让我不舒服  | `short_term_state` 或 `life_event` | `profile_candidate` | 一次情绪不是画像    |
| 我现在在客户现场   | `short_term_state`                | `task_create`       | 当前场景，不是未来任务 |

## 保存策略

建议：

```text
short_term_state 默认自动保存
必须可见、可删除、有有效期
```

默认有效期：

| 状态表达     | 有效期建议        |
| -------- | ------------ |
| 今天       | 当天           |
| 现在       | 几小时或当天       |
| 最近 / 这几天 | 3-7 天        |
| 这段时间     | 7 天，除非用户明确更久 |

---

# 4. `life_event`：生活事件 / 经验记录

## 白话定义

已经发生的事情、一次经历、一次教训、一次经验、一次值得以后参考的记录。

它和任务的区别是：

```text
task_create：未来要做
life_event：已经发生 / 已经学到
```

## 判断条件

符合以下情况，判断为 `life_event`：

1. 今天发生了某件事。
2. 用户得到一个经验。
3. 用户说下次要注意。
4. 用户记录一次客户沟通、做菜、出差、会议、错误。
5. 事情已经发生，不需要未来提醒。
6. 不是稳定偏好，只是一次经历。

## 真实例句

| 例句                     | 分类                                | 自动保存建议        | 说明          |
| ---------------------- | --------------------------------- | ------------- | ----------- |
| 今天做番茄炒蛋糖放多了，下次少放       | `life_event`                      | 可自动保存或低风险自动保存 | 一次经验        |
| 这次出差忘了订酒店，下次提前订        | `life_event`                      | 可自动保存         | 出差教训        |
| 今天客户沟通不太顺利             | `life_event` 或 `short_term_state` | 看表达重点         | 事件 + 情绪     |
| 今天同学约我去网吧，我去了          | `life_event`                      | 可自动保存         | 已发生社交事件     |
| 今天开会发现张总更关注成本          | `life_event`                      | 可自动保存         | 客户经验        |
| 这次猪场项目沟通时，现场负责人更在意落地方案 | `life_event`                      | 可自动保存         | 项目经验        |
| 下次见客户前要提前准备案例          | `life_event` 或 `task_create`      | 取决于是否有具体下次    | 泛化经验，不是具体任务 |

## 容易混淆边界

| 句子             | 正确分类                               | 不应分类为               | 原因          |
| -------------- | ---------------------------------- | ------------------- | ----------- |
| 明天去客户现场        | `task_create`                      | `life_event`        | 未来行动        |
| 今天去了客户现场       | `life_event`                       | `task_create`       | 已发生         |
| 今天客户说话让我不舒服    | `short_term_state` + `life_event`  | `profile_candidate` | 不应变成长期画像    |
| 我以后见客户都要提前准备案例 | `profile_candidate` 或 `life_event` | `task_create`       | 可能是长期工作方式候选 |
| 记住，下次做菜少放糖     | `life_event`                       | `profile_candidate` | 一次经验，不是长期画像 |

## 保存策略

你说得对，不能只靠“记一下/记住”。

建议改为：

```text
只要用户表达了“这件事以后有参考价值”，就可以保存 life_event。
```

包括：

```text
下次注意
以后要记得
长了个教训
以后别再
发现一个问题
这次经验是
以后可以
```

但也不能纯关键词，最终还是语义判断。

---

# 5. `profile_candidate`：长期画像候选

## 白话定义

用户明确表达了一个长期稳定的偏好、习惯、背景、工作方式、沟通方式。

它只是候选，不能自动生效。

## 判断条件

符合以下情况，判断为 `profile_candidate`：

1. 用户说“我一般……”
2. 用户说“我长期……”
3. 用户说“我不喜欢……”
4. 用户说“我更适合……”
5. 用户说“我习惯……”
6. 用户描述稳定背景，比如经常出差、长期负责某类客户。
7. 用户表达明确偏好，比如提醒频率、沟通风格、工作时间。

## 真实例句

| 例句            | 分类                  | 自动保存建议 | 说明     |
| ------------- | ------------------- | ------ | ------ |
| 我不喜欢太频繁的提醒    | `profile_candidate` | 必须确认   | 稳定提醒偏好 |
| 我一般晚上效率比较高    | `profile_candidate` | 必须确认   | 稳定工作节奏 |
| 我经常出差         | `profile_candidate` | 必须确认   | 长期背景   |
| 我希望别人说话直接一点   | `profile_candidate` | 必须确认   | 沟通偏好   |
| 我不太喜欢早上被催     | `profile_candidate` | 必须确认   | 稳定偏好   |
| 我平时更适合下午处理重要事 | `profile_candidate` | 必须确认   | 工作节奏   |
| 我负责的客户大多是猪场项目 | `profile_candidate` | 必须确认   | 长期工作背景 |

## 容易混淆边界

| 句子            | 正确分类                              | 不应分类为               | 原因          |
| ------------- | --------------------------------- | ------------------- | ----------- |
| 我今天很累         | `short_term_state`                | `profile_candidate` | 单次状态        |
| 最近有点拖延        | `short_term_state`                | `profile_candidate` | 近期状态，不是长期习惯 |
| 今天客户让我不舒服     | `short_term_state` / `life_event` | `profile_candidate` | 一次事件        |
| 我一直不喜欢别人催我    | `profile_candidate`               | `short_term_state`  | 稳定偏好        |
| 这次出差忘订酒店，下次注意 | `life_event`                      | `profile_candidate` | 一次经验        |

## 保存策略

必须坚持：

```text
profile_candidate 永远不能自动生效
必须用户确认
```

当前 schema 也强制 `profile_candidate.need_user_confirm === true`，这个规则要保留。

---

# 6. `general_answer`：普通问答

## 白话定义

用户是在问问题、聊天、寻求建议，但没有表达需要保存的任务、状态、事件或画像。

## 判断条件

符合以下情况，判断为 `general_answer`：

1. 纯问题。
2. 闲聊。
3. 让 AI 分析。
4. 让 AI 给建议。
5. 没有明确未来行动。
6. 没有要保存的个人事实。
7. 没有当前状态记录价值。

## 真实例句

| 例句          | 分类               | 保存建议            | 说明   |
| ----------- | ---------------- | --------------- | ---- |
| 番茄炒蛋怎么做好吃？  | `general_answer` | 不保存             | 普通问答 |
| 你觉得我应该换工作吗？ | `general_answer` | 不默认保存           | 咨询问题 |
| 网吧适合学习吗？    | `general_answer` | 不保存             | 普通判断 |
| 今天适合出门吗？    | `general_answer` | 不保存             | 问答   |
| 怎么和客户沟通更好？  | `general_answer` | 不保存             | 建议请求 |
| 帮我分析一下这个项目  | `general_answer` | 不保存，除非用户提供可记忆事实 | 分析请求 |

## 混合情况

如果用户说：

```text
我今天和客户沟通不顺，你觉得我该怎么办？
```

可以拆成：

```text
life_event 或 short_term_state：今天客户沟通不顺
general_answer：你觉得我该怎么办
```

也就是说，一个输入可以拆多个 item。

---

# 自动保存总规则

我建议 D0 之后改成下面这个策略。

| 类型                     | 自动保存？ | 条件                  |
| ---------------------- | ----- | ------------------- |
| `task_create`          | 可以    | 明确未来行动 / 安排 / 已答应事项 |
| `task_update.complete` | 可以    | 唯一高置信匹配任务           |
| `task_update.cancel`   | 可以    | 唯一高置信匹配任务，但不直接隐藏    |
| `task_update.delay`    | 谨慎可以  | 目标任务明确 + 新时间明确      |
| `task_update.edit`     | 谨慎可以  | 目标任务明确 + 修改内容明确     |
| `short_term_state`     | 可以    | 当前/今天/近期状态          |
| `life_event`           | 可以但谨慎 | 明确已发生且有未来参考价值       |
| `profile_candidate`    | 不可以   | 永远必须确认              |
| `general_answer`       | 不可以   | 默认不保存               |

---

# D0 需要补的真实中文例句方向

你后面可以按这几类继续给我例句。

## 1. 社交 / 娱乐安排

```text
晚上同学约我去网吧，我同意了
周末和朋友吃饭
明晚陪老婆去逛街
下午跟朋友打球
晚上去看电影
```

核心判断：未来安排 → `task_create`

---

## 2. 工作 / 客户任务

```text
明天去猪场看项目
下午给张总发方案
这两天整理一下客户资料
下周找王总确认进场时间
月底前把会议资料准备好
```

核心判断：未来行动 → `task_create`

---

## 3. 当前状态

```text
我现在在路上
今天有点累
这两天睡眠不好
最近项目有点乱
我现在不方便打电话
```

核心判断：当前/近期状态 → `short_term_state`

---

## 4. 已发生事件 / 经验

```text
今天客户沟通不顺
这次出差忘订酒店
今天做菜糖放多了
今天开会发现客户更关注成本
这次项目推进太急了，下次要提前对齐
```

核心判断：已发生 + 有参考价值 → `life_event`

---

## 5. 长期画像

```text
我不喜欢频繁提醒
我一般晚上效率高
我经常出差
我更适合下午处理重要事情
我不喜欢别人说话绕弯子
```

核心判断：稳定偏好/习惯/背景 → `profile_candidate`

---

## 6. 任务更新

```text
王总那个事做完了
明天会议取消
把客户沟通推到后天
联系王总改成联系张总
晚上去网吧这个事不去了
```

核心判断：改变已有任务 → `task_update`

---

# 我建议下一步怎么做

下一步我们就继续做 D0，不急着写代码。

你可以直接给我一批真实例句，不需要整理得很规范，就像你平时会对这个 App 说话一样。

我会帮你逐条整理成这个格式：

```text
原句：
分类：
是否自动保存：
是否需要用户确认：
title/content 应该是什么：
tags 建议：
Flash 容易误判：
prompt 规则建议：
sample set 写法：
```

然后我们积累到 40-60 条以后，再交给 Codex 去改：

```text
parserPrompt.ts
parserSampleSet.ts
smokeParseSamples.ts
移动端 task_update 自动应用和任务状态展示
```

最终目标不是让模型记关键词，而是让 Flash 学会：

> **这句话在这个产品里，未来应该被当作什么使用。**
